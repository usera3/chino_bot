# 系统诊断功能 - 验证指南

## 功能概述

系统诊断功能让机器人具备"全局视角"，能够在工具调用失败时查看日志进行自我诊断。

**新增工具**：
- `view_logs` - 查看系统日志（NapCat 或机器人日志）
- `check_system_status` - 检查系统运行状态

**工具总数**：31 → 33 个

---

## 验证方法

### 方法 1：检查启动日志 ✅

查看机器人启动时是否加载了系统工具：

```bash
tail -50 logs/bot.log | grep "系统工具"
```

**预期输出**：
```
✅ 已加载: 系统工具
✅ 已加载 33 个工具
```

---

### 方法 2：运行集成测试 ✅

```bash
cd zhinai-bot-v3
python test_system_tools_integration.py
```

**测试场景**：
1. 主动查看日志
2. 检查系统状态
3. 模拟工具失败后的自我诊断

**预期结果**：
- Butler 能够调用 `view_logs` 工具
- Butler 能够调用 `check_system_status` 工具
- Butler 能够分析日志并给出友好的反馈

---

### 方法 3：实际使用测试 ✅

在 QQ 中 @ 机器人，发送以下测试消息：

#### 测试 1：查看日志
```
@机器人 帮我看看 NapCat 的最新 5 条日志
```

**预期行为**：
- 机器人调用 `view_logs(log_type="qq", lines=5)`
- 返回日志内容或分析结果

#### 测试 2：检查系统状态
```
@机器人 检查一下系统状态
```

**预期行为**：
- 机器人调用 `check_system_status()`
- 返回进程、端口、内存、磁盘等信息

#### 测试 3：自我诊断（核心功能）
```
@机器人 我刚才发送文件失败了，帮我看看是什么原因
```

**预期行为**：
1. 机器人识别到这是一个问题诊断请求
2. 调用 `view_logs(log_type="qq", lines=10)` 查看 NapCat 日志
3. 分析日志中的错误信息
4. 给出友好的解释（不直接暴露日志内容）

**示例回复**：
```
我看了一下日志，发现是因为文件路径不存在导致的。
你可以检查一下文件是否在正确的位置，或者重新上传试试~
```

---

## 验证清单

### ✅ 基础验证
- [ ] 启动日志显示"✅ 已加载: 系统工具"
- [ ] 工具总数为 33 个
- [ ] 独立测试脚本运行成功（`test_system_tools.py`）

### ✅ 集成验证
- [ ] Butler 能够调用 `view_logs` 工具
- [ ] Butler 能够调用 `check_system_status` 工具
- [ ] Butler 能够分析日志并给出友好反馈

### ✅ 实际使用验证
- [ ] 在 QQ 中能够查看日志
- [ ] 在 QQ 中能够检查系统状态
- [ ] 在工具失败后能够自我诊断

---

## 快速验证命令

### 1. 检查工具是否加载
```bash
tail -50 logs/bot.log | grep -E "(系统工具|已加载.*工具)"
```

### 2. 运行完整测试
```bash
cd zhinai-bot-v3
python test_system_tools_integration.py
```

### 3. 查看实时日志（观察工具调用）
```bash
tail -f logs/bot.log | grep -E "(view_logs|check_system_status|调用工具)"
```

---

## 预期日志输出

### 启动时
```
✅ 已加载: 文件管理工具
✅ 已加载: 系统工具
✅ 已加载: 工作流工具
✅ 已加载 33 个工具
```

### 调用 view_logs 时
```
🤖 Agent 推理过程:
   🔧 调用工具: view_logs
   📥 输入: {'log_type': 'qq', 'lines': 5}
   📤 工具输出: 📋 NapCat日志（最新 5 行）...
```

### 调用 check_system_status 时
```
🤖 Agent 推理过程:
   🔧 调用工具: check_system_status
   📥 输入: {}
   📤 工具输出: 🔍 进程状态...
```

---

## 故障排查

### 问题 1：工具未加载
**症状**：启动日志中没有"系统工具"

**解决方案**：
```bash
# 检查文件是否存在
ls -la zhinai-bot-v3/tools/system_tools.py

# 检查是否有语法错误
python -m py_compile zhinai-bot-v3/tools/system_tools.py

# 重启机器人
bash /path/to/chino_bot/start_bot.sh
```

### 问题 2：psutil 模块未安装
**症状**：`check_system_status` 工具报错

**解决方案**：
```bash
pip install psutil
```

### 问题 3：日志文件路径错误
**症状**：`view_logs` 返回"日志文件不存在"

**解决方案**：
检查 `tools/system_tools.py` 中的日志路径是否正确：
```python
log_file = "/path/to/chino_bot/logs/qq.log"
```

---

## 使用建议

### 何时使用系统诊断工具

**推荐场景**：
- 工具调用失败后（如 send_file 失败）
- 用户报告功能异常
- 需要排查问题原因

**不推荐场景**：
- 正常对话中频繁调用
- 用户没有遇到问题时主动查看
- 作为常规信息展示

### 最佳实践

1. **只在出错时使用**
   - 工具调用失败 → 查看日志
   - 不要在正常情况下频繁调用

2. **分析后再反馈**
   - 不要直接把日志内容发给用户
   - 分析错误原因，给出友好的解释
   - 提供解决方案或建议

3. **控制日志行数**
   - 默认 5 行足够
   - 复杂问题可以增加到 10-20 行
   - 不要一次读取太多日志

---

## 示例对话

### 场景 1：文件发送失败

**用户**：我刚才发送文件失败了

**机器人思考过程**：
1. 识别到这是问题诊断请求
2. 调用 `view_logs(log_type="qq", lines=10)`
3. 分析日志发现：`[error] 文件不存在: /path/to/file.txt`
4. 给出友好反馈

**机器人回复**：
```
我看了一下日志，发现是因为文件路径不存在。
可能是文件已经被删除或者路径不正确。
你可以重新上传文件试试~
```

### 场景 2：系统状态检查

**用户**：机器人状态怎么样

**机器人思考过程**：
1. 调用 `check_system_status()`
2. 获取系统信息
3. 格式化输出

**机器人回复**：
```
系统运行正常~
✅ 机器人进程运行中
✅ QQ 连接正常
💾 内存使用：10.6 GB / 24.0 GB (62.8%)
💿 磁盘空间：175 GB 可用
```

---

## 技术细节

### 工具实现

**view_logs**：
- 使用 `tail` 命令读取日志文件
- 支持 QQ 日志和机器人日志
- 可配置读取行数

**check_system_status**：
- 使用 `psutil` 库获取系统信息
- 检查进程、端口、内存、磁盘
- 格式化输出便于阅读

### Butler 集成

在 `core/butler.py` 的系统提示词中添加了使用指南：
- 何时调用工具
- 如何分析日志
- 如何给出友好反馈

---

## 更新记录

**2026-01-19**：
- ✅ 创建系统诊断工具
- ✅ 集成到 Butler
- ✅ 添加测试脚本
- ✅ 编写验证指南

---

## 相关文件

- `tools/system_tools.py` - 系统工具实现
- `test_system_tools.py` - 基础测试
- `test_system_tools_integration.py` - 集成测试
- `系统诊断功能说明.md` - 功能说明
- `系统诊断功能-验证指南.md` - 本文档
