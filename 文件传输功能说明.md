# 文件传输功能说明

## 功能概述

机器人现在支持 QQ 群文件的上传和查看功能，基于 OneBot V11 标准 API 实现。

## 支持的功能

### 1. 上传群文件

**工具名称**：`upload_group_file`

**功能**：将本地文件上传到 QQ 群文件

**使用场景**：
- 用户说"上传xxx文件到群里"
- 用户说"把xxx文件发到群文件"
- 用户说"分享xxx文件"

**参数**：
- `file_path`：本地文件路径（必需）
- `name`：文件名称（必需）
- `group_id`：群号（可选，不提供时使用当前群）
- `folder`：上传到的文件夹路径（可选，默认为根目录 `/`）

**示例**：
```python
# 上传文件到当前群的根目录
await bot.call_api(
    "upload_group_file",
    group_id=878812866,
    file="/path/to/file.pdf",
    name="文档.pdf"
)

# 上传文件到指定文件夹
await bot.call_api(
    "upload_group_file",
    group_id=878812866,
    file="/path/to/file.pdf",
    name="文档.pdf",
    folder="/资料"
)
```

**注意事项**：
- 文件必须存在于本地
- 文件大小限制取决于 QQ 群文件限制（通常为 2GB）
- 只能在群聊中使用
- 需要有上传权限

### 2. 查看群文件列表

**工具名称**：`get_group_files`

**功能**：获取 QQ 群文件列表

**使用场景**：
- 用户问"群里有什么文件"
- 用户说"查看群文件"
- 用户说"列出群文件"

**参数**：
- `group_id`：群号（可选，不提供时使用当前群）
- `folder_id`：文件夹 ID（可选，不提供则获取根目录）

**返回信息**：
- 文件夹列表（名称、ID）
- 文件列表（名称、大小）

**示例**：
```python
# 获取根目录文件
result = await bot.call_api(
    "get_group_root_files",
    group_id=878812866
)

# 获取子目录文件
result = await bot.call_api(
    "get_group_files_by_folder",
    group_id=878812866,
    folder_id="folder_id_here"
)
```

## OneBot V11 支持的其他文件 API

### 3. 获取群文件系统信息

```python
# 获取群文件系统信息（总空间、已用空间等）
result = await bot.call_api(
    "get_group_file_system_info",
    group_id=878812866
)
```

### 4. 获取群文件下载链接

```python
# 获取群文件下载链接
result = await bot.call_api(
    "get_group_file_url",
    group_id=878812866,
    file_id="file_id_here",
    busid=102  # 固定值
)
```

### 5. 上传私聊文件

```python
# 上传文件到私聊
await bot.call_api(
    "upload_private_file",
    user_id=123456789,
    file="/path/to/file.pdf",
    name="文档.pdf"
)
```

## 使用示例

### 示例 1：用户上传文件

```
用户：@智乃 把 /Users/xxx/document.pdf 上传到群文件，名字叫"重要文档.pdf"

机器人推理过程：
1. 识别用户意图：上传文件
2. 提取参数：
   - file_path: /Users/xxx/document.pdf
   - name: 重要文档.pdf
   - group_id: 从当前群获取
3. 调用 upload_group_file 工具
4. 返回结果

机器人回复：
✅ 文件上传成功！
📁 文件名：重要文档.pdf
📊 大小：2.5 MB
📂 位置：群 878812866 的 /
```

### 示例 2：查看群文件

```
用户：@智乃 群里有什么文件？

机器人推理过程：
1. 识别用户意图：查看群文件
2. 调用 get_group_files 工具
3. 解析返回结果
4. 格式化输出

机器人回复：
📂 群 878812866 的文件列表：

📁 文件夹：
  - 资料 (ID: xxx)
  - 图片 (ID: yyy)

📄 文件：
  - 重要文档.pdf (2.5 MB)
  - 会议记录.docx (1.2 MB)
```

## 接收文件

当用户在群里发送文件时，机器人会收到文件消息事件：

```python
# 在 chat_plugin.py 中处理文件消息
for seg in event.message:
    if seg.type == "file":
        file_id = seg.data.get("file_id")
        file_name = seg.data.get("name")
        file_size = seg.data.get("size")
        
        # 可以获取文件下载链接
        file_url = await bot.call_api(
            "get_group_file_url",
            group_id=event.group_id,
            file_id=file_id,
            busid=102
        )
```

## 技术实现

### 1. 工具定义

在 `tools/file_transfer_tool.py` 中定义了两个 LangChain Tool：
- `UploadGroupFileTool`：上传群文件
- `GetGroupFilesTool`：获取群文件列表

### 2. Bot 实例管理

通过全局变量管理 Bot 实例：
```python
_bot_instance = None

def set_bot_instance(bot):
    global _bot_instance
    _bot_instance = bot

def get_bot_instance():
    return _bot_instance
```

### 3. 集成到 Butler

在 `tools/basic_tools.py` 中集成：
```python
from .file_transfer_tool import get_all_file_transfer_tools

file_tools = get_all_file_transfer_tools()
all_tools.extend(file_tools)
```

### 4. 设置 Bot 实例

在 `plugins/chat_plugin.py` 中设置：
```python
from tools.file_transfer_tool import set_bot_instance as set_file_bot_instance

set_file_bot_instance(bot)
```

## 限制和注意事项

1. **文件大小限制**
   - QQ 群文件通常限制为 2GB
   - 具体限制取决于群等级和会员等级

2. **权限要求**
   - 机器人需要有上传文件的权限
   - 某些群可能限制普通成员上传文件

3. **文件路径**
   - 文件必须存在于机器人运行的服务器上
   - 用户无法直接上传本地文件（需要先发送给机器人）

4. **API 支持**
   - 依赖 OneBot V11 标准 API
   - 需要 go-cqhttp 或 NapCat 等实现支持

5. **跨机器部署**
   - 如果 NoneBot 和 go-cqhttp 部署在不同机器上
   - 可能需要使用 `nonebot-plugin-gocqhttp-cross-machine-upload-file` 插件

## 未来扩展

1. **下载文件**
   - 实现从群文件下载到本地
   - 支持文件处理（如解压、转换等）

2. **私聊文件**
   - 支持上传文件到私聊
   - 支持接收私聊文件

3. **文件管理**
   - 创建文件夹
   - 删除文件
   - 移动文件

4. **智能推理**
   - 用户发送文件后自动上传到群文件
   - 根据文件类型自动分类到不同文件夹

## 参考资料

- [OneBot V11 标准 API](https://github.com/botuniverse/onebot-11)
- [NoneBot2 文档](https://nonebot.dev/)
- [go-cqhttp 文档](https://docs.go-cqhttp.org/)
- [NapCat 文档](https://napneko.github.io/)
