# 好友申请自动处理功能

## 📋 功能说明

智乃机器人现在支持自动处理好友申请，包括：
- ✅ 自动同意好友申请
- 📝 发送欢迎消息
- 🚫 黑名单过滤
- ✅ 白名单模式
- 🔍 关键词过滤
- 📢 通知超级用户

## 🚀 快速开始

### 1. 功能已自动启用

好友申请插件已经集成到机器人中，重启机器人即可生效：

```bash
# 停止机器人
bash stop_bot.sh

# 启动机器人
bash start_bot.sh
```

### 2. 默认行为

默认配置下，机器人会：
- ✅ 自动同意所有好友申请
- 📝 发送欢迎消息
- 📊 记录日志

### 3. 测试功能

让朋友向机器人发送好友申请，机器人会：
1. 自动同意申请
2. 发送欢迎消息
3. 在日志中记录

查看日志：
```bash
tail -f ../logs/bot.log | grep "好友申请"
```

## ⚙️ 配置说明

配置文件位置：`config/friend_request_config.py`

### 基础配置

```python
# 是否自动同意好友申请
AUTO_APPROVE = True  # True: 自动同意, False: 不自动同意

# 是否发送欢迎消息
SEND_WELCOME = True  # True: 发送, False: 不发送

# 欢迎消息内容（可自定义）
WELCOME_MESSAGE = """你好呀！我是智乃，很高兴认识你~

我可以帮你做很多事情：
• 💬 聊天对话
• 🌤️ 查询天气
• 📧 发送邮件
• 🔍 网络搜索
• 🖼️ 图片理解
• ⏰ 定时提醒

直接和我说话就可以啦！有什么需要帮助的吗？"""

# 自动同意时的备注名（留空则不设置）
AUTO_REMARK = ""
```

### 黑名单配置

拒绝特定用户的好友申请：

```python
# 黑名单（这些 QQ 号不会自动同意）
BLACKLIST = [
    123456789,  # 添加不想同意的 QQ 号
    987654321,
]
```

### 白名单模式

只同意白名单中的用户：

```python
# 启用白名单模式
WHITELIST_MODE = True

# 白名单
WHITELIST = [
    111111111,  # 只同意这些 QQ 号
    222222222,
]
```

### 关键词过滤

根据申请备注过滤：

```python
# 拒绝关键词（备注包含这些词会被拒绝）
REJECT_KEYWORDS = [
    "广告",
    "推广",
    "加群",
    "代理",
]

# 必须关键词（备注必须包含其中之一才会同意）
REQUIRED_KEYWORDS = [
    "朋友推荐",
    "GitHub",
    "技术交流",
]
```

### 通知配置

```python
# 是否记录所有好友申请到日志
LOG_REQUESTS = True

# 是否通知超级用户有新的好友申请
NOTIFY_SUPERUSER = True  # 会发送私聊消息通知
```

## 📊 使用示例

### 示例 1：完全开放

```python
AUTO_APPROVE = True
SEND_WELCOME = True
BLACKLIST = []
WHITELIST_MODE = False
REJECT_KEYWORDS = []
REQUIRED_KEYWORDS = []
```

**效果**：同意所有好友申请，发送欢迎消息

### 示例 2：黑名单模式

```python
AUTO_APPROVE = True
SEND_WELCOME = True
BLACKLIST = [123456789, 987654321]  # 拒绝这些用户
WHITELIST_MODE = False
```

**效果**：同意除黑名单外的所有申请

### 示例 3：白名单模式

```python
AUTO_APPROVE = True
SEND_WELCOME = True
BLACKLIST = []
WHITELIST_MODE = True
WHITELIST = [111111111, 222222222]  # 只同意这些用户
```

**效果**：只同意白名单中的用户

### 示例 4：关键词过滤

```python
AUTO_APPROVE = True
SEND_WELCOME = True
REJECT_KEYWORDS = ["广告", "推广"]  # 拒绝包含这些词的申请
REQUIRED_KEYWORDS = ["朋友推荐"]    # 必须包含这个词
```

**效果**：
- 备注包含"广告"或"推广" → 拒绝
- 备注不包含"朋友推荐" → 拒绝
- 其他 → 同意

### 示例 5：严格模式

```python
AUTO_APPROVE = True
SEND_WELCOME = True
WHITELIST_MODE = True
WHITELIST = [111111111, 222222222]
REJECT_KEYWORDS = ["广告"]
REQUIRED_KEYWORDS = ["验证码"]
NOTIFY_SUPERUSER = True
```

**效果**：
- 必须在白名单中
- 备注不能包含"广告"
- 备注必须包含"验证码"
- 通知超级用户

## 🔍 日志查看

### 查看所有好友申请

```bash
tail -f ../logs/bot.log | grep "好友申请"
```

### 查看同意的申请

```bash
tail -f ../logs/bot.log | grep "已自动同意"
```

### 查看拒绝的申请

```bash
tail -f ../logs/bot.log | grep "已拒绝"
```

## 📝 日志示例

### 同意申请

```
[INFO] 收到好友申请: 用户 123456789, 备注: 朋友推荐
[SUCCESS] ✓ 已自动同意用户 123456789 的好友申请 (原因: 通过所有检查)
[SUCCESS] ✓ 已向用户 123456789 发送欢迎消息
```

### 拒绝申请（黑名单）

```
[INFO] 收到好友申请: 用户 987654321, 备注: 你好
[WARNING] ✗ 已拒绝用户 987654321 的好友申请 (原因: 用户在黑名单中)
```

### 拒绝申请（关键词）

```
[INFO] 收到好友申请: 用户 111111111, 备注: 广告推广
[WARNING] ✗ 已拒绝用户 111111111 的好友申请 (原因: 备注包含拒绝关键词: 广告)
```

## 🛠️ 高级功能

### 自定义欢迎消息

可以根据不同情况发送不同的欢迎消息：

```python
# 在 friend_request_plugin.py 中修改
WELCOME_MESSAGE = f"""你好呀！我是智乃 🎀

欢迎添加我为好友！我是一个智能助手，可以帮你：

📱 基础功能
• 💬 聊天对话 - 随时和我聊天
• 🌤️ 天气查询 - 查询任意城市天气
• 🔍 网络搜索 - 帮你搜索信息

🔧 高级功能
• 📧 邮件助手 - 发送和管理邮件
• 🖼️ 图片理解 - 分析图片内容
• ⏰ 定时提醒 - 设置定时任务

💡 使用提示
直接和我说话就可以啦！比如：
"北京天气怎么样？"
"帮我搜索 Python 教程"
"给 xxx@qq.com 发封邮件"

有什么需要帮助的吗？"""
```

### 动态备注名

根据申请备注设置好友备注：

```python
# 在配置中
AUTO_REMARK = ""  # 留空

# 在插件中动态设置
if "GitHub" in comment:
    remark = "GitHub用户"
elif "朋友推荐" in comment:
    remark = "朋友推荐"
else:
    remark = ""
```

### 统计功能

记录好友申请统计：

```python
# 可以添加到插件中
import json
from datetime import datetime

stats_file = "data/friend_request_stats.json"

def record_request(user_id, comment, approved):
    stats = {
        "user_id": user_id,
        "comment": comment,
        "approved": approved,
        "timestamp": datetime.now().isoformat()
    }
    # 保存到文件
    with open(stats_file, "a") as f:
        f.write(json.dumps(stats) + "\n")
```

## ⚠️ 注意事项

1. **首次使用**
   - 修改配置后需要重启机器人
   - 建议先测试再正式使用

2. **黑白名单**
   - 黑名单优先级高于白名单
   - 白名单模式会拒绝所有不在白名单中的用户

3. **关键词过滤**
   - 关键词匹配区分大小写
   - 可以使用正则表达式（需要修改代码）

4. **欢迎消息**
   - 消息不要太长，避免被限制
   - 可以包含表情和换行

5. **通知功能**
   - 需要在 .env 中配置 SUPERUSERS
   - 通知消息会发送到所有超级用户

## 🐛 故障排查

### 问题 1：好友申请没有自动同意

**检查：**
```bash
# 1. 查看日志
tail -f ../logs/bot.log | grep "好友申请"

# 2. 检查配置
cat config/friend_request_config.py | grep AUTO_APPROVE

# 3. 确认插件已加载
tail -f ../logs/bot.log | grep "friend_request"
```

**解决：**
- 确保 `AUTO_APPROVE = True`
- 检查是否在黑名单中
- 检查白名单模式设置

### 问题 2：欢迎消息没有发送

**检查：**
```bash
# 查看日志
tail -f ../logs/bot.log | grep "欢迎消息"
```

**解决：**
- 确保 `SEND_WELCOME = True`
- 检查机器人是否有发送消息权限
- 等待时间可能需要调整（默认 2 秒）

### 问题 3：配置修改不生效

**解决：**
```bash
# 重启机器人
bash stop_bot.sh
bash start_bot.sh

# 清除 Python 缓存
find . -name "*.pyc" -delete
find . -name "__pycache__" -delete
```

## 📚 相关文档

- [NoneBot2 文档](https://nonebot.dev/)
- [OneBot11 适配器](https://onebot.adapters.nonebot.dev/)
- [好友申请事件](https://onebot.adapters.nonebot.dev/docs/api/v11/event#FriendRequestEvent)

## 🎉 总结

好友申请自动处理功能让机器人更加智能和自动化：
- ✅ 自动同意好友申请，无需手动处理
- 📝 发送欢迎消息，提升用户体验
- 🛡️ 黑白名单和关键词过滤，保护账号安全
- 📊 完整的日志记录，方便追踪

有问题或建议？欢迎提 Issue！
