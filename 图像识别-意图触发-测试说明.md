# 图像识别 - 意图触发模式 - 测试说明

## 📋 更新内容

### ✅ 已完成的修改

1. **vision_tool.py** - 重构为符合 Function Calling Agent 标准的工具
   - 继承 `BaseTool` 基类
   - 实现标准接口：`get_name()`, `get_description()`, `get_parameters()`, `execute()`
   - 返回 `ToolResult` 对象

2. **function_calling_agent.py** - 注册 vision_understanding 工具
   - 添加到工具列表
   - 更新系统提示词，说明何时使用图片识别

3. **langchain_chat.py** - 提取图片URL并传递给AI
   - 自动提取消息中的图片URL
   - 将图片URL附加到消息上下文中
   - 格式：`[图片1 URL: https://...]`

4. **image_handler.py** - 禁用自动识别
   - 不再自动识别所有图片
   - 改为由AI根据用户意图决定是否调用

## 🎯 工作原理

### 旧模式（已禁用）
- ❌ 用户发送**任何图片** → 自动调用API识别 → 自动回复
- 问题：浪费API调用、干扰正常对话、不符合真实聊天习惯

### 新模式（当前）
- ✅ 用户发送图片 + 文字询问 → AI判断意图 → 如需要则调用工具
- 优点：
  - 节省API调用
  - 符合真实聊天习惯
  - AI可以理解上下文
  - 支持针对性的问题回答

## 🧪 测试场景

### 场景1：发送图片 + 询问内容
**输入：**
```
@机器人 [图片] 这是什么？
```

**预期行为：**
1. 聊天插件提取图片URL
2. 将消息和图片URL一起传递给AI
3. AI识别用户意图（询问图片）
4. 调用 `vision_understanding` 工具
5. 返回图片识别结果

**日志特征：**
```
[LangChain Chat] 收到来自 xxx 的消息: 这是什么？ [包含 1 张图片]
[LangChain Chat] 附带 1 张图片的URL
[Function Calling Agent] 🎯 AI决定调用工具: vision_understanding
🖼️ 开始理解图片: https://...
✅ 图片理解成功: ...
```

### 场景2：发送图片 + 具体问题
**输入：**
```
@机器人 [图片] 图片里有几个人？
```

**预期行为：**
- AI调用 `vision_understanding` 工具
- 传递用户的具体问题：`question="图片里有几个人？"`
- 返回针对性的回答

### 场景3：发送图片 + 闲聊
**输入：**
```
@机器人 [图片] 你好呀
```

**预期行为：**
1. AI收到消息和图片URL
2. 判断用户意图是闲聊，不是询问图片
3. **不调用**图片识别工具
4. 直接回复 "你好呀" 等闲聊内容

**日志特征：**
```
[LangChain Chat] 收到来自 xxx 的消息: 你好呀 [包含 1 张图片]
[Function Calling Agent] AI 原始回复: 你好呀！...
（没有工具调用）
```

### 场景4：纯文本消息
**输入：**
```
@机器人 今天天气怎么样？
```

**预期行为：**
- 正常处理，不涉及图片
- 可能调用 `get_weather` 工具

### 场景5：仅发送图片（无文字）
**输入：**
```
@机器人 [图片]
```

**预期行为：**
1. 聊天插件检测到只有图片，没有文字
2. 构造消息：`"用户发送了图片\n[图片1 URL: ...]"`
3. AI可能会主动询问或简单回应
4. **不会**自动调用识别（除非AI主动想看）

## 📊 对比测试

| 测试用例 | 旧模式（自动） | 新模式（意图） |
|---------|--------------|--------------|
| 发送图片 "这是什么" | ✅ 自动识别 | ✅ 识别（意图明确） |
| 发送图片 "帮我看看" | ✅ 自动识别 | ✅ 识别（意图明确） |
| 发送图片 "你好" | ✅ 自动识别（浪费） | ❌ 不识别（正确） |
| 发送图片（无文字） | ✅ 自动识别 | ❌ 不识别（节省） |
| 发送10张表情包 | ✅ 全部识别（浪费） | ❌ 不识别（正确） |

## 🔍 验证方法

### 1. 查看启动日志
```bash
tail -f logs/bot.log | grep -E "(vision|图片|图像)"
```

确认工具已注册：
```
[Function Calling Agent] 已注册工具：[..., 'vision_understanding']
🖼️ 图像识别已整合到 Function Calling Agent（基于用户意图触发）
```

### 2. 实时监控日志
```bash
tail -f logs/bot.log
```

发送测试消息，观察：
- 是否提取到图片URL
- AI是否决定调用工具
- 工具是否成功执行

### 3. 功能测试清单
- [ ] 发送图片 + "这是什么" → 应该识别
- [ ] 发送图片 + "图片里有什么" → 应该识别
- [ ] 发送图片 + "你好" → 不应该识别
- [ ] 仅发送图片 → 不应该识别
- [ ] 发送文字（无图片） → 正常对话

## 🎉 优势总结

1. **节省API成本** - 只在需要时调用
2. **符合人类习惯** - 不会对每张图都做评论
3. **支持复杂问答** - 可以针对用户问题回答
4. **减少干扰** - 不会在群聊中频繁插嘴
5. **灵活扩展** - 易于添加更多视觉相关功能

## 📝 注意事项

1. **图片URL有效期**
   - QQ图片URL可能有时效性
   - 需要在有效期内调用

2. **API限流**
   - Gemini API 有rate limit
   - 错误码500通常表示限流

3. **群聊权限**
   - 需要@机器人或回复机器人的消息
   - 避免在群里频繁触发

## 🚀 下一步优化方向

1. **图片缓存** - 缓存识别结果，避免重复调用
2. **批量识别** - 支持一次处理多张图片
3. **OCR增强** - 专门的文字识别模式
4. **图片生成** - 集成图片生成工具
5. **图片搜索** - 以图搜图功能

---

**测试完成时间：** 2025-10-26
**版本：** v1.0 - 意图触发模式


