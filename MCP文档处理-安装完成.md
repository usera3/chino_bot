# MCP 文档处理工具 - 安装完成

## 安装状态

✅ **已成功安装并配置 MCP 文档处理工具！**

## 已安装的 MCP 服务器

### 1. Doc Forge ✅

**包名**: `@cablate/mcp-doc-forge`

**功能**：
- ✅ 读取多种文档格式（DOCX、PDF、TXT、HTML、CSV）
- ✅ 文档转换（DOCX → HTML/PDF、HTML → TXT/Markdown）
- ✅ PDF 合并和分割
- ✅ 文本清理和对比
- ✅ HTML 资源提取

**配置位置**: `.kiro/settings/mcp.json`

**自动批准的工具**:
- `read_document` - 读取文档
- `convert_document` - 转换文档格式

## 配置文件

```json
{
  "mcpServers": {
    "doc-forge": {
      "command": "npx",
      "args": ["-y", "@cablate/mcp-doc-forge"],
      "disabled": false,
      "autoApprove": ["read_document", "convert_document"]
    }
  }
}
```

## 可用功能

### 1. 读取文档

机器人现在可以读取以下格式的文档：
- 📄 PDF 文件
- 📝 Word 文档（DOCX）
- 📋 文本文件（TXT）
- 🌐 HTML 文件
- 📊 CSV 文件

**使用示例**：
```
用户：@智乃 帮我读一下这个 PDF 文件
[用户发送 PDF 文件]

机器人：
📄 文档内容摘要：
这是一份关于...的文档，主要内容包括...
```

### 2. 转换文档格式

支持的转换：
- DOCX → HTML
- DOCX → PDF
- HTML → TXT
- HTML → Markdown
- PDF → Markdown

**使用示例**：
```
用户：@智乃 把这个 Word 文档转成 PDF

机器人：
✅ 转换成功！
📁 原文件：文档.docx
📁 新文件：文档.pdf
```

### 3. PDF 操作

- 合并多个 PDF 文件
- 分割 PDF 文件
- 提取 PDF 文本

**使用示例**：
```
用户：@智乃 把这两个 PDF 合并成一个

机器人：
✅ PDF 合并成功！
📁 新文件：合并文档.pdf
📊 总页数：25 页
```

### 4. 文本处理

- 清理文本（去除多余空格、换行等）
- 文本对比（diff）
- HTML 资源提取

## 工作流示例

### 工作流 1：接收文档 → 阅读 → 回答问题

```
1. 用户发送 PDF 文件到群里
2. 用户问：@智乃 这个文档讲的是什么？
3. 机器人：
   - 下载 PDF 文件
   - 使用 MCP 工具读取内容
   - 理解并总结
   - 回答用户问题
```

### 工作流 2：转换文档 → 上传群文件

```
1. 用户：@智乃 把这个 Word 转成 PDF
2. 机器人：
   - 接收 Word 文件
   - 使用 MCP 工具转换为 PDF
   - 上传到群文件
   - 通知用户完成
```

### 工作流 3：合并文档 → 分享

```
1. 用户：@智乃 把这几个 PDF 合并成一个
2. 机器人：
   - 接收多个 PDF 文件
   - 使用 MCP 工具合并
   - 上传到群文件
   - 通知用户完成
```

## 智能推理能力

机器人会自动推理用户的意图：

**示例 1：隐式读取**
```
用户：@智乃 这个文档说了什么？
[用户发送 PDF]

机器人推理：
1. 识别用户发送了文件
2. 推理用户想要了解文件内容
3. 自动调用 read_document 工具
4. 总结并回答
```

**示例 2：隐式转换**
```
用户：@智乃 我需要一个 PDF 版本
[之前发送了 Word 文档]

机器人推理：
1. 从上下文中找到 Word 文档
2. 推理用户想要转换格式
3. 自动调用 convert_document 工具
4. 生成 PDF 并分享
```

## 与其他功能的结合

### 1. 文件传输 + 文档处理

```
用户：@智乃 群文件里有个报告.docx，帮我转成 PDF 然后发给我

机器人：
1. 使用 get_group_files 查找文件
2. 下载 报告.docx
3. 使用 convert_document 转换为 PDF
4. 上传到群文件或发送给用户
```

### 2. 图像识别 + 文档处理

```
用户：@智乃 这张图片上的文字是什么？
[用户发送图片]

机器人：
1. 使用图像识别工具识别文字
2. 可选：创建 Word 文档保存识别结果
3. 上传到群文件
```

### 3. 定时任务 + 文档处理

```
用户：@智乃 每天早上9点把昨天的聊天记录整理成 Word 文档

机器人：
1. 创建定时任务
2. 每天9点执行：
   - 提取聊天记录
   - 创建 Word 文档
   - 上传到群文件
```

## 注意事项

1. **首次使用**
   - MCP 服务器会在首次使用时自动下载依赖
   - 可能需要几秒到几分钟的时间
   - 这是正常现象，请耐心等待

2. **文件大小限制**
   - 建议处理的文件不超过 50MB
   - 大文件可能需要较长处理时间

3. **支持的格式**
   - 确保文件格式正确
   - 损坏的文件可能无法处理

4. **权限**
   - 机器人需要有读写文件的权限
   - 确保工作目录可写

## 测试建议

### 测试 1：读取 PDF

1. 发送一个 PDF 文件到群里
2. @智乃 这个 PDF 讲的是什么？
3. 观察机器人是否能正确读取并总结

### 测试 2：转换格式

1. 发送一个 Word 文档
2. @智乃 把这个转成 PDF
3. 观察机器人是否能正确转换

### 测试 3：合并 PDF

1. 发送两个 PDF 文件
2. @智乃 把这两个 PDF 合并
3. 观察机器人是否能正确合并

## 故障排除

### 问题 1：MCP 服务器启动失败

**解决方案**：
```bash
# 检查 npx 是否可用
npx --version

# 手动测试 MCP 服务器
npx -y @cablate/mcp-doc-forge --help
```

### 问题 2：文档读取失败

**可能原因**：
- 文件格式不支持
- 文件损坏
- 文件过大

**解决方案**：
- 确认文件格式
- 尝试较小的文件
- 查看错误日志

### 问题 3：转换失败

**可能原因**：
- 源文件格式问题
- 目标格式不支持
- 依赖缺失

**解决方案**：
- 检查文件格式
- 查看 MCP 服务器日志
- 重启机器人

## 下一步

1. **测试功能**：尝试上面的测试建议
2. **探索更多**：尝试不同的文档操作
3. **反馈问题**：如果遇到问题，查看日志并反馈
4. **扩展功能**：根据需要添加更多 MCP 服务器

## 相关文档

- `MCP文档处理工具推荐.md` - 完整的工具推荐和配置指南
- `文件传输功能说明.md` - 文件传输功能说明
- `智能推理能力设计.md` - 智能推理能力设计

## 总结

✅ MCP 文档处理工具已成功安装
✅ 机器人现在可以读取和处理文档
✅ 支持多种文档格式和转换
✅ 与文件传输功能完美结合
✅ 具备智能推理能力

机器人现在是一个完整的文档处理助手！🎉
