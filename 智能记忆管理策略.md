# 智能记忆管理策略

## 🎯 核心理念

**让大模型自己判断是否需要长期记忆，而不是用固定的关键词规则。**

## 📋 完整流程

```
用户消息
   ↓
1. 判断是否是简单互动（戳一戳、系统消息）
   ↓
2. 如果不是简单互动 → 让大模型判断是否需要长期记忆
   ↓
3. 如果需要 → 检索长期记忆（VectorStore）
   ↓
4. 构建消息上下文
   - 系统提示词
   - 最近对话（1-3轮）
   - 当前消息（可能包含历史记忆）
   ↓
5. 调用 LangChain Agent 生成回复
   ↓
6. 保存到记忆
   - 短期记忆（chat_history）
   - 长期记忆（VectorStore，带时间戳）
```

## 🤖 大模型判断逻辑

### 判断提示词

```
你是一个智能助手，需要判断用户的消息是否需要查询历史对话记忆。

用户消息："{user_input}"

判断标准：
- 如果用户明确提到过去的事情（"之前"、"刚才"、"上次"、"记得"、"还记得"）→ 需要
- 如果用户询问个人信息（"我叫什么"、"我的名字"、"我是谁"）→ 需要
- 如果是简单问候、闲聊、新话题 → 不需要
- 如果是独立的问题，不依赖历史 → 不需要

请只回答"需要"或"不需要"，不要有其他内容。
```

### 判断示例

| 用户消息 | 大模型判断 | 原因 |
|---------|-----------|------|
| "你好" | 不需要 | 简单问候 |
| "吃饭了吗" | 不需要 | 独立问题 |
| "你还记得我之前说的吗" | 需要 | 明确提到"之前"、"记得" |
| "我叫什么名字" | 需要 | 询问个人信息 |
| "今天天气怎么样" | 不需要 | 独立问题 |
| "刚才那个问题" | 需要 | 提到"刚才" |

## 🧠 记忆管理

### 1. 短期记忆（chat_history）

**策略：每次重新构建消息列表**

```python
# 不使用持久的 chat_history，每次构建新的
messages = [
    SystemMessage(system_prompt),
    ...最近对话（1-3轮）,
    HumanMessage(当前消息)
]

# 传给 Agent
agent.ainvoke({"messages": messages})

# 保存到 chat_history（用于下次构建）
chat_history.append(当前消息)
chat_history.append(AI回复)

# 定期清理
if 简单互动:
    保留最近2轮
else:
    保留最近5轮
```

**优势：**
- ✅ 防止 AI 被旧话题困住
- ✅ 每次对话相对独立
- ✅ 能够自然切换话题

### 2. 长期记忆（VectorStore）

**策略：按需检索，大模型判断**

#### 存储格式（带时间戳）

```python
conversation_text = "[2025-01-18 18:42:30] 用户: 你好\n智乃: 下午好呀~"

metadata = {
    "user_id": "1446437177",
    "timestamp": "2025-01-18 18:42:30",
    "type": "conversation"
}
```

#### 检索呈现（带时间差）

```
[历史对话 1] （刚才）
[2025-01-18 18:42:30] 用户: 你好
智乃: 下午好呀~

[历史对话 2] （5分钟前）
[2025-01-18 18:37:15] 用户: 你在干嘛
智乃: 发呆呢...
```

**优势：**
- ✅ 大模型智能判断，比关键词规则更灵活
- ✅ 简单对话不会被无关历史干扰
- ✅ AI 能够区分"刚才"和"很久以前"

## 📊 不同场景的处理

### 场景 1：简单问候
```
用户: "你好"
→ 跳过大模型判断（简单互动）
→ 不检索长期记忆
→ 只使用最近 2-3 轮对话
→ 回复简短自然
```

### 场景 2：戳一戳
```
用户: [空白 戳了戳你]
→ 跳过大模型判断（简单互动）
→ 不检索长期记忆
→ 只使用最近 1 轮对话
→ 回复简短可爱
```

### 场景 3：独立问题
```
用户: "今天天气怎么样"
→ 大模型判断：不需要
→ 不检索长期记忆
→ 使用最近 2-3 轮对话
→ 直接回答问题
```

### 场景 4：需要历史信息
```
用户: "你还记得我之前说的吗"
→ 大模型判断：需要 ✅
→ 检索长期记忆
→ 显示时间差（5分钟前、2小时前）
→ 结合历史信息回复
```

### 场景 5：连续对话
```
用户: "下午好"
智乃: "下午好呀~"
用户: "吃饭了吗"
→ 大模型判断：不需要
→ 不检索长期记忆
→ 使用最近 2-3 轮对话（包含"下午好"）
→ 但因为每次重新构建，不会被"下午好"困住
```

## 🔑 关键优势

### 1. 智能判断
- 大模型比固定关键词更灵活
- 能够理解上下文和语义
- 减少误判

### 2. 防止困住
- 每次重新构建消息列表
- 不累积 Agent 状态
- 能够自然切换话题

### 3. 时间感知
- 历史记忆带时间戳
- 显示时间差（刚才、5分钟前、2小时前）
- AI 能够区分时间

### 4. 性能优化
- 简单互动跳过判断
- 不需要时不检索
- 减少不必要的 API 调用

## 📈 性能考虑

### API 调用次数

**旧策略（关键词规则）：**
- 每次消息：1 次 API 调用（生成回复）

**新策略（大模型判断）：**
- 简单互动：1 次 API 调用（生成回复）
- 普通消息：2 次 API 调用（判断 + 生成回复）
- 需要历史：2 次 API 调用 + 1 次向量检索

**优化建议：**
- 判断使用更快的模型（如 DeepSeek-Chat）
- 判断提示词简短，响应快
- 缓存判断结果（相同消息类型）

## 🎯 未来优化方向

1. **缓存判断结果** - 相似消息类型不重复判断
2. **多级记忆** - 短期、中期、长期记忆分层
3. **记忆重要性评分** - 重要对话保留更久
4. **用户画像** - 根据用户习惯调整策略
5. **A/B 测试** - 对比不同策略的效果

## 📝 总结

这个策略通过**让大模型自己判断**，实现了更智能、更灵活的记忆管理。它平衡了：
- **上下文连贯性** - 能够记住最近的对话
- **话题切换灵活性** - 不会被旧话题困住
- **性能** - 按需检索，不浪费资源
- **智能性** - 大模型判断比固定规则更准确
