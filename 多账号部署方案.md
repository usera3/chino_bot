# 多 QQ 账号部署方案

## 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    Nginx (可选)                          │
│              负载均衡 + 反向代理                          │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
┌───────▼──────┐  ┌───────▼──────┐  ┌───────▼──────┐
│  Bot 1       │  │  Bot 2       │  │  Bot 3       │
│  QQ:2509xxx  │  │  QQ:3456xxx  │  │  QQ:7890xxx  │
│  Port:8080   │  │  Port:8081   │  │  Port:8082   │
│  NapCat:3000 │  │  NapCat:3001 │  │  NapCat:3002 │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
                ┌─────────▼─────────┐
                │  共享数据库        │
                │  - ChromaDB       │
                │  - SQLite         │
                └───────────────────┘
```

## 实现步骤

### 1. 目录结构

```
zhinai-bot-v3/
├── bots/                    # 多账号配置目录
│   ├── bot1/
│   │   ├── .env            # Bot1 配置
│   │   └── napcat/         # NapCat 数据
│   ├── bot2/
│   │   ├── .env
│   │   └── napcat/
│   └── bot3/
│       ├── .env
│       └── napcat/
├── data/                    # 共享数据（按 user_id 隔离）
│   ├── conversations/
│   └── knowledge/
├── logs/                    # 日志目录
│   ├── bot1.log
│   ├── bot2.log
│   └── bot3.log
├── bot.py                   # NoneBot 主程序
├── start_multi_bots.sh      # 多账号启动脚本
└── stop_multi_bots.sh       # 多账号停止脚本
```

### 2. 配置文件示例

#### bots/bot1/.env
```env
# Bot 基础配置
BOT_ID=bot1
BOT_QQ=1000000000
HOST=127.0.0.1
PORT=8080
NAPCAT_PORT=3000

# NoneBot 配置
LOG_LEVEL=INFO
SUPERUSERS=["123456789"]
NICKNAME=["智乃", "chino"]

# API 配置（共享）
DEEPSEEK_API_KEY=sk-xxx
QQ_EMAIL_SENDER=1000000000@qq.com
QQ_EMAIL_PASSWORD=your_qq_smtp_authorization_code
```

#### bots/bot2/.env
```env
BOT_ID=bot2
BOT_QQ=3456789012
HOST=127.0.0.1
PORT=8081
NAPCAT_PORT=3001

# 其他配置同上...
```

### 3. 启动脚本

#### start_multi_bots.sh
```bash
#!/bin/bash

# 定义所有 Bot 配置
declare -A BOTS=(
    ["bot1"]="8080:3000:1000000000"
    ["bot2"]="8081:3001:3456789012"
    ["bot3"]="8082:3002:7890123456"
)

# 启动所有 Bot
for bot_id in "${!BOTS[@]}"; do
    IFS=':' read -r port napcat_port qq <<< "${BOTS[$bot_id]}"
    
    echo "启动 $bot_id (QQ:$qq, Port:$port)..."
    
    # 启动 NoneBot
    cd bots/$bot_id
    nohup python ../../bot.py > ../../logs/${bot_id}.log 2>&1 &
    echo $! > ../../.${bot_id}_nonebot.pid
    
    # 启动 NapCat
    nohup napcat --port $napcat_port > ../../logs/${bot_id}_napcat.log 2>&1 &
    echo $! > ../../.${bot_id}_napcat.pid
    
    cd ../..
    
    echo "✓ $bot_id 启动成功"
done

echo "所有 Bot 启动完成！"
```

### 4. 管理脚本

#### stop_multi_bots.sh
```bash
#!/bin/bash

for pid_file in .bot*_nonebot.pid .bot*_napcat.pid; do
    if [ -f "$pid_file" ]; then
        pid=$(cat "$pid_file")
        kill $pid 2>/dev/null
        rm "$pid_file"
    fi
done

echo "所有 Bot 已停止"
```

#### status_multi_bots.sh
```bash
#!/bin/bash

echo "Bot 状态："
for pid_file in .bot*_nonebot.pid; do
    bot_id=$(echo $pid_file | sed 's/\.//g' | sed 's/_nonebot\.pid//g')
    if [ -f "$pid_file" ]; then
        pid=$(cat "$pid_file")
        if ps -p $pid > /dev/null; then
            echo "✓ $bot_id: 运行中 (PID: $pid)"
        else
            echo "✗ $bot_id: 已停止"
        fi
    fi
done
```

## 优势

1. **隔离性**：每个 Bot 独立运行，互不影响
2. **稳定性**：一个崩溃不影响其他
3. **可扩展**：轻松添加新账号
4. **易管理**：可独立启动/停止/重启
5. **数据共享**：所有 Bot 共享数据库（按 user_id 隔离）

## 注意事项

1. **端口分配**：确保每个 Bot 使用不同端口
2. **资源限制**：每个 Bot 约占用 200-500MB 内存
3. **数据隔离**：虽然共享数据库，但按 user_id 自动隔离
4. **日志管理**：定期清理日志文件
5. **监控告警**：建议添加进程监控

## 下一步

1. 创建 bots 目录结构
2. 配置各个 Bot 的 .env 文件
3. 修改启动脚本支持多实例
4. 测试多账号运行
