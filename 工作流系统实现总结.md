# 工作流系统实现总结

## 🎉 实现完成

已成功实现完整的工作流系统，替代了简单的 `schedule_task` 工具，提供了更强大和可扩展的任务调度能力。

## ✅ 已完成的工作

### 1. 数据模型 (`models/workflow_models.py`)
- `WorkflowDefinition` - 工作流定义
- `WorkflowStep` - 工作流步骤
- `StepResult` - 步骤执行结果
- `WorkflowStatus` - 工作流状态枚举
- `StepStatus` - 步骤状态枚举
- `ErrorStrategy` - 错误处理策略枚举

### 2. 工作流执行器 (`core/workflow_executor.py`)
- 按依赖关系排序步骤（拓扑排序）
- 解析模板变量（`{{variable}}`）
- 执行工具并收集输出
- 错误处理和重试机制
- 支持 LangChain Tool 和普通函数

### 3. 工作流调度器 (`core/workflow_scheduler.py`)
- 解析触发时间（相对时间和绝对时间）
- 异步等待并触发执行
- 工作流管理（注册、取消、查询）
- 全局单例模式

### 4. 工作流工具 (`tools/workflow_tool.py`)
- AI 创建工作流的接口
- 验证步骤定义
- 生成友好的确认消息
- 集成到 LangChain Tool 系统

### 5. 工具注册表 (`tools/langchain_tools.py`)
- `build_tool_registry()` - 构建工具名称到实例的映射
- 支持工作流工具访问所有可用工具

### 6. 集成到 Butler (`tools/basic_tools.py`)
- 在 `get_all_tools()` 中添加工作流工具
- 自动构建工具注册表
- 传递给工作流工具

### 7. 用户上下文注入 (`plugins/chat_plugin.py`)
- 在消息处理时设置 `current_user_id`
- 在消息处理时设置 `current_group_id`
- 确保工作流知道是谁创建的

### 8. 系统提示词更新 (`core/butler.py`)
- 移除了"定时任务不能发邮件"的限制
- 添加了 `create_workflow` 工具的使用说明
- 推荐使用工作流处理定时任务

### 9. 文档
- `工作流系统说明.md` - 详细的系统文档
- `定时任务功能说明.md` - 更新的功能说明
- `工作流系统实现总结.md` - 本文档

### 10. 测试
- `test_workflow.py` - 工作流系统测试脚本
- 测试单步骤工作流
- 测试多步骤工作流
- 测试数据传递

## 🧪 测试结果

### 测试 1：单步骤工作流 - 定时发邮件
```
用户：10秒后给我发邮件提醒吃饭
AI：设置好啦，10秒后会给你发邮件提醒吃饭的 (｡･ω･｡)

工作流：
- 名称：吃饭提醒
- 触发时间：10秒后
- 步骤：
  1. send_email

结果：✅ 成功创建并执行
```

### 测试 2：多步骤工作流 - 先查天气再发邮件
```
用户：15秒后查深圳天气然后发邮件告诉我
AI：设置完成啦，15秒后会先查深圳天气，然后发邮件告诉你的 (｡･ω･｡)ﾉ♡

工作流：
- 名称：深圳天气查询并通知
- 触发时间：15秒后
- 步骤：
  1. get_weather → weather_info
  2. send_email (使用 {{weather_info}})

结果：✅ 成功创建
```

## 🎯 核心特性

### 1. 多步骤执行
- ✅ 支持按顺序执行多个工具
- ✅ 每个步骤可以调用不同的工具
- ✅ 步骤间可以传递数据

### 2. 数据传递
- ✅ 使用 `output_key` 保存步骤输出
- ✅ 使用 `{{variable}}` 模板语法引用输出
- ✅ 自动替换模板变量

### 3. 时间调度
- ✅ 相对时间：`10秒后`、`5分钟后`、`2小时后`
- ✅ 绝对时间：`12:24`、`14:30:00`
- ✅ 自动处理时区（东八区）

### 4. 错误处理
- ✅ continue：步骤失败后继续执行
- ✅ stop：步骤失败后停止工作流
- ✅ 自动重试（默认 3 次）
- ✅ 指数退避策略

### 5. 依赖管理
- ✅ 使用 `depends_on` 指定依赖关系
- ✅ 拓扑排序确保执行顺序
- ✅ 验证依赖合法性

## 📊 与旧版对比

| 特性 | 旧版 schedule_task | 新版 create_workflow |
|------|-------------------|---------------------|
| 单步骤任务 | ✅ | ✅ |
| 多步骤任务 | ❌ | ✅ |
| 数据传递 | ❌ | ✅ |
| 错误处理 | 基础 | 高级 |
| 重试机制 | ❌ | ✅ |
| 依赖管理 | ❌ | ✅ |
| 扩展性 | 低 | 高 |
| 可维护性 | 低 | 高 |

## 🏗️ 架构设计

```
用户请求
    ↓
Butler Agent (AI)
    ↓
create_workflow Tool
    ↓
WorkflowScheduler (调度器)
    ↓
异步等待触发时间
    ↓
WorkflowExecutor (执行器)
    ↓
按顺序执行步骤
    ↓
调用工具 (Tool Registry)
    ↓
收集输出并传递给下一步
    ↓
完成工作流
```

## 💡 设计亮点

### 1. 使用 LangChain 的工作流能力
- 不是简单地添加更多 action 类型
- 而是让工具之间可以联动
- 符合 LangChain 的设计理念

### 2. 工具注册表模式
- 所有工具统一管理
- 工作流可以调用任何已注册的工具
- 易于扩展新工具

### 3. 模板变量系统
- 简单直观的 `{{variable}}` 语法
- 自动替换和类型转换
- 支持嵌套数据结构

### 4. 异步执行
- 使用 asyncio 实现非阻塞调度
- 支持并发执行多个工作流
- 资源自动清理

### 5. 错误处理
- 多层次的错误处理
- 自动重试机制
- 灵活的错误策略

## 🚀 未来扩展方向

### 短期计划
- [ ] 工作流持久化（保存到数据库）
- [ ] 工作流取消和修改接口
- [ ] 工作流执行历史查询

### 中期计划
- [ ] 条件分支（if-else）
- [ ] 循环执行（for/while）
- [ ] 并行执行（parallel steps）

### 长期计划
- [ ] 工作流模板系统
- [ ] 可视化工作流编辑器
- [ ] 工作流市场（分享和复用）

## 📝 使用建议

### 对于 AI
1. **优先使用工作流**
   - 单步骤任务：推荐使用 `create_workflow`
   - 多步骤任务：必须使用 `create_workflow`

2. **生成完整参数**
   - 不要让用户补充参数
   - 自动生成邮件主题和内容
   - 使用描述性的 output_key

3. **合理设计数据流**
   - 使用模板变量传递数据
   - 避免过于复杂的工作流
   - 考虑失败场景

### 对于开发者
1. **添加新工具**
   - 在 `tools/` 目录添加工具
   - 在 `get_all_tools()` 中注册
   - 自动加入工具注册表

2. **扩展工作流功能**
   - 修改 `WorkflowExecutor` 添加新特性
   - 修改 `WorkflowScheduler` 添加新调度策略
   - 保持向后兼容

3. **调试和测试**
   - 使用 `test_workflow.py` 测试
   - 查看详细的执行日志
   - 验证错误处理

## 🎓 技术要点

### 1. 工具注册表
```python
# 构建工具注册表
tool_registry = build_tool_registry(all_tools)

# 工作流工具需要访问所有工具
workflow_tool = get_workflow_tool(tool_registry)
```

### 2. 用户上下文注入
```python
# 在消息处理时设置
for tool in butler.tools:
    if tool.name == 'create_workflow':
        tool.current_user_id = user_id
        tool.current_group_id = group_id
```

### 3. 模板变量替换
```python
# 在执行器中自动替换
content = "天气情况：{{weather_info}}"
# 替换后
content = "天气情况：📍 广东 深圳\n🌡️ 温度：25°C..."
```

### 4. 异步调度
```python
# 创建异步任务
task = asyncio.create_task(self._wait_and_execute(workflow, delay))
self.tasks[workflow_id] = task
```

## ✅ 总结

工作流系统的实现是一个重要的里程碑：

1. **解决了用户的问题**
   - 用户说"2分钟后发邮件"现在可以正常工作
   - 支持更复杂的多步骤任务

2. **采用了正确的架构**
   - 使用 LangChain 的工作流能力
   - 工具之间可以联动
   - 可持续发展的设计

3. **提供了良好的扩展性**
   - 易于添加新工具
   - 易于扩展新特性
   - 保持代码整洁

4. **保持了向后兼容**
   - 旧的 `schedule_task` 仍然可用
   - 平滑迁移路径
   - 不影响现有功能

这是一个优雅的解决方案，比简单地在 `schedule_task` 中添加更多 action 类型要好得多！🎉
