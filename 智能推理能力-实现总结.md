# 智能推理能力 - 实现总结

## 问题背景

用户提出了一个深刻的问题：

> "假如有个第一次和机器人说话的人让机器人给他发个邮件，机器人会先询问你的邮件是什么，还是说会调用查看qq信息的工具自动推理他的qq邮箱是qq号加上qq邮箱的后缀，然后再进一步给这个人发邮件呢，后者显然看起来更智能"

这不仅仅是邮件的问题，而是**无数复杂问题的一个缩影**。

**核心问题**：如何让机器人拥有**推理复杂问题的能力**？

## 解决方案

### 1. 设计哲学转变

从"规则驱动"转向"推理驱动"：

**❌ 旧方式（规则驱动）**：
```python
if user_input == "给我发个邮件":
    if not has_email:
        ask_for_email()
    else:
        send_email()
```

**✅ 新方式（推理驱动）**：
```
通过系统提示词教会 AI：
1. 主动推理，不被动询问
2. 多步骤思考
3. 利用上下文信息
4. 智能补全缺失信息
5. 常见推理模式
```

### 2. 核心实现

#### 2.1 系统提示词增强

在 `core/butler.py` 中，添加了**核心推理能力**部分：

```python
## 🧠 核心推理能力（最重要）

你不是一个简单的问答机器人，而是一个**具有推理能力的智能助手**。

### 推理原则

1. **主动推理，不要被动询问**
   - 用户说"给我发个邮件" → 不要问"你的邮箱是什么"
   - 应该推理：用户的 QQ 号 → QQ 邮箱格式（QQ号@qq.com）→ 发送邮件
   - 工具链：get_user_info → 提取 QQ 号 → 构造邮箱 → send_email

2. **多步骤思考**
   - 遇到复杂问题，分解成多个步骤
   - 每个步骤可能需要调用不同的工具
   - 工具之间可以串联使用

3. **利用上下文信息**
   - 系统会在消息中注入用户的 QQ 号、群号等信息
   - 充分利用这些信息进行推理
   - 例如：[系统提示：用户的 QQ 号是 1446437177] → 可以推理邮箱是 1446437177@qq.com

4. **智能补全缺失信息**
   - 用户没有提供的信息，尝试通过工具获取或推理
   - 例如：用户说"给我点赞"但没说 QQ 号 → 从上下文获取当前用户 QQ 号
   - 例如：用户说"发邮件"但没说邮箱 → 通过 QQ 号推理邮箱

5. **常见推理模式**
   - QQ 号 → QQ 邮箱：`{qq_number}@qq.com`
   - 用户说"我" → 当前对话用户
   - 用户说"给xxx" → 如果 xxx 被 @，使用被 @ 的用户
   - 时间相关 → 可能需要 get_time 工具
   - 天气相关 → 可能需要 get_weather 工具
```

#### 2.2 推理示例

提供了具体的推理示例：

**示例 1：智能发邮件**
```
用户："给我发个邮件"
❌ 错误：回复"你的邮箱是什么？"
✅ 正确：
  1. 从系统提示中看到用户 QQ 号是 1446437177
  2. 推理邮箱地址：1446437177@qq.com
  3. 调用 send_email 工具
  4. 回复"已经发到你的 QQ 邮箱啦~"
```

**示例 2：智能点赞**
```
用户："给 @张三 点赞"
❌ 错误：回复"张三的 QQ 号是什么？"
✅ 正确：
  1. 从消息中识别被 @ 的用户（系统已经处理好了）
  2. 直接调用 send_like 工具（工具会自动使用被 @ 的用户）
  3. 回复"已经给张三点赞啦~"
```

**示例 3：复杂推理**
```
用户："10分钟后给我发个邮件提醒吃饭"
✅ 正确：
  1. 识别这是定时任务 + 发邮件
  2. 推理邮箱地址（从 QQ 号）
  3. 调用 create_workflow 工具，设置：
     - trigger_time: "10分钟后"
     - steps: [send_email 工具]
     - 邮件内容：提醒吃饭
  4. 回复"好的，10分钟后会发邮件提醒你吃饭~"
```

#### 2.3 工具使用哲学

重新设计了工具使用规则，从"规则列表"转向"推理模式"：

```python
## 🛠️ 工具使用哲学

⚠️ **核心原则**：工具是用来**解决问题**的，不是用来**回答问题**的！

### 何时调用工具

**判断标准**：用户的意图是否需要**执行某个操作**或**获取实时信息**？

✅ **需要调用工具**：
- 用户要求执行操作（发邮件、点赞、画图、搜索图片）
- 用户询问实时信息（现在几点、天气如何、我叫什么）
- 用户设置定时任务（10秒后提醒我）
- 用户发送链接（需要解析内容）
- 用户发送图片（需要识别内容）

❌ **不需要调用工具**：
- 用户告诉你信息（"我的邮箱是xxx" → 只需记住）
- 用户询问你已知的信息（"你记得我的邮箱吗" → 直接回答）
- 用户闲聊（"今天天气真好" → 不需要调用 get_weather）
- 用户分享感受（"我喜欢xxx" → 不需要调用任何工具）

### 工具使用的推理模式

**模式 1：直接调用**
用户："现在几点？" → 调用 get_time 工具

**模式 2：推理后调用**
用户："给我发个邮件" → 推理邮箱地址（从 QQ 号）→ 调用 send_email 工具

**模式 3：多工具串联**
用户："查一下我的信息然后发邮件给我"
→ 调用 get_user_info 工具
→ 从结果中提取 QQ 号
→ 推理邮箱地址
→ 调用 send_email 工具

**模式 4：条件判断**
用户："给我点赞"
→ 检查上下文中是否有用户 QQ 号
→ 如果有，直接调用 send_like
→ 如果没有，先调用 get_user_info
```

### 3. 技术架构

#### 3.1 上下文注入

在 `plugins/chat_plugin.py` 中，系统会自动注入用户信息：

```python
context_info = f"[系统提示：发送此消息的用户是{sender_nickname}(QQ:{user_id})"
if group_id:
    context_info += f"，当前在群{group_id}中"
context_info += f"。你的 QQ 号是 {bot_qq}，用户的 QQ 号是 {user_id}]"

enhanced_user_input = context_info + "\n\n" + user_input
```

#### 3.2 工具自动推理

工具设计支持参数缺失时的自动推理：

```python
class SendLikeTool(BaseTool):
    def _run(self, user_id: Optional[str] = None, times: int = 10):
        # 如果没有提供 user_id，自动从上下文获取
        if not user_id:
            context = get_current_context()
            # 优先使用被 @ 的用户
            mentioned_users = context.get("mentioned_users", [])
            if mentioned_users:
                user_id = mentioned_users[0]
            else:
                # 否则使用当前用户
                user_id = context.get("user_id")
        
        # 执行点赞
        ...
```

#### 3.3 LangGraph Agent

使用 LangGraph 的 ReAct Agent，天然支持：
- 多步推理
- 工具串联
- 中间结果传递

```python
self.agent = create_react_agent(
    model=self.llm,
    tools=self.tools,
    prompt=self.system_prompt
)
```

## 效果对比

### 场景 1：发邮件

**机械式**：
```
用户：给我发个邮件
机器人：你的邮箱是什么？
用户：1446437177@qq.com
机器人：好的，已发送
```
- 需要 2 轮对话
- 用户体验差

**智能式**：
```
用户：给我发个邮件
机器人：已经发到你的 QQ 邮箱啦~
```
- 只需 1 轮对话
- 用户体验好

### 场景 2：复杂任务

**机械式**：
```
用户：10分钟后给我发个邮件提醒吃饭
机器人：你的邮箱是什么？
用户：1446437177@qq.com
机器人：好的，已设置
```

**智能式**：
```
用户：10分钟后给我发个邮件提醒吃饭
机器人：好的，10分钟后会发邮件提醒你吃饭~
```

## 关键要点

1. **不要写死规则**
   - 不要为每个场景写死"如果xxx就xxx"的规则
   - 让 AI 自己学会推理

2. **依赖 AI 推理**
   - 通过好的系统提示词教会 AI 推理
   - 提供具体的推理示例

3. **提供推理示例**
   - 在系统提示词中提供具体的推理示例
   - 示例比规则更有效

4. **工具支持自动推理**
   - 工具设计要支持参数缺失时的自动推理
   - 从上下文中获取信息

5. **充分利用上下文**
   - 注入足够的上下文信息供 AI 推理
   - QQ 号、群号、被 @ 的用户等

## 扩展性

这个推理框架可以扩展到更多场景：

1. **智能搜索**：用户说"帮我查一下北京的天气" → 推理需要天气信息 → 调用 get_weather

2. **智能提醒**：用户说"明天早上8点提醒我开会" → 推理定时任务 + 时间计算 → 调用 create_workflow

3. **智能绘图**：用户说"画一个知识图谱" → 推理需要生成图片 → 调用 render_html

4. **智能搜图**：用户说"给我看看猫的图片" → 推理需要搜索图片 → 调用 search_image

5. **复杂任务串联**：用户说"查一下天气，如果下雨就发邮件提醒我带伞" → 多步推理 + 条件判断

## 文件清单

- `zhinai-bot-v3/core/butler.py` - 核心推理能力实现
- `zhinai-bot-v3/plugins/chat_plugin.py` - 上下文注入
- `zhinai-bot-v3/tools/qq_interaction_tools.py` - 工具自动推理
- `zhinai-bot-v3/智能推理能力设计.md` - 设计哲学文档
- `zhinai-bot-v3/test_smart_email.py` - 测试脚本

## 测试方法

1. **测试智能发邮件**：
   ```
   用户：@智乃 给我发个邮件
   预期：机器人自动推理邮箱地址并发送，不询问邮箱
   ```

2. **测试智能点赞**：
   ```
   用户：@智乃 给 @张三 点赞
   预期：机器人自动识别被 @ 的用户并点赞，不询问 QQ 号
   ```

3. **测试复杂推理**：
   ```
   用户：@智乃 10分钟后给我发个邮件提醒吃饭
   预期：机器人自动推理邮箱地址并设置定时任务，不询问邮箱
   ```

## 总结

**智能推理能力是 AI 助手的核心竞争力。**

通过：
- ✅ 好的系统提示词设计（推理原则 + 推理示例）
- ✅ 充分的上下文注入（QQ 号、群号等）
- ✅ 支持自动推理的工具（参数缺失时自动获取）
- ✅ 强大的 Agent 框架（LangGraph ReAct Agent）

我们让机器人从"问答机器"进化为"推理助手"，提供更智能、更自然的用户体验。

**这不仅仅是技术问题，更是设计哲学的问题。**
