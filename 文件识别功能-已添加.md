# 文件识别功能 - 已添加 ✅

## 问题
用户发送文件后，机器人没有识别到文件，只看到空消息。

## 原因
`chat_plugin.py` 只处理了文本、图片、@ 和回复消息，没有处理文件消息（`seg.type == "file"`）。

## 解决方案

### 1. 添加文件消息解析
```python
elif seg.type == "file":
    # 获取文件信息
    file_name = seg.data.get("file", "未知文件")
    file_id = seg.data.get("file_id", "")
    file_size = seg.data.get("file_size", 0)
    file_info.append({
        "name": file_name,
        "id": file_id,
        "size": file_size
    })
```

### 2. 添加文件信息到系统提示
```python
# 添加文件信息
if file_info:
    context_info += f"，发送了{len(file_info)}个文件："
    for f in file_info:
        file_size_mb = f['size'] / (1024 * 1024)
        context_info += f"\n  - {f['name']} ({file_size_mb:.2f} MB, ID: {f['id']})"
```

### 3. 自动添加默认提示
```python
# 如果没有文本但有文件，添加默认提示
if not user_input and file_info:
    file_names = ", ".join([f['name'] for f in file_info])
    user_input = f"请阅读这些文件：{file_names}"
```

### 4. 添加日志输出
```python
if file_info:
    print(f"   文件: {len(file_info)} 个")
    for f in file_info:
        print(f"     - {f['name']} ({f['size']/1024:.2f} KB)")
```

## 测试结果

现在机器人可以：
- ✅ 识别用户发送的文件
- ✅ 提取文件名、ID、大小
- ✅ 在系统提示中告知 AI 有文件
- ✅ 在日志中显示文件信息

## 下一步

机器人现在能识别文件了，但还需要：
1. **文件下载工具** - 下载文件到本地
2. **文件读取工具** - 已有（`read_word`, `read_pdf`, `read_excel`）

### 需要创建的工具
- `download_file` - 下载 QQ 文件到本地
  - 输入：file_id, file_name
  - 输出：本地文件路径
  - 然后可以使用现有的 `read_word` 等工具读取

## 文件处理流程

1. 用户发送文件 → 机器人识别文件信息
2. AI 决定调用 `download_file` 工具
3. 文件下载到本地（如 `~/Downloads/`）
4. AI 调用 `read_word` 或 `read_pdf` 读取内容
5. AI 分析内容并回复用户

## 注意事项

- 文件 ID 可以用于下载文件
- 需要使用 OneBot API 的 `get_group_file_url` 或类似接口
- 下载的文件应保存到 `~/Downloads/` 目录（沙盒可访问）
