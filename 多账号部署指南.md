# 多 QQ 账号部署指南

## 快速开始

### 1. 创建第一个 Bot 配置

```bash
bash create_bot_config.sh
```

按提示输入：
- Bot 名称：bot1
- QQ 号：2509109290
- NoneBot 端口：8080
- NapCat 端口：3000

### 2. 编辑配置文件

```bash
vim bots/bot1/.env
```

修改以下配置：
- `QQ_EMAIL_PASSWORD`: QQ 邮箱授权码
- `DEEPSEEK_API_KEY`: DeepSeek API 密钥（如需要）

### 3. 启动所有 Bot

```bash
bash start_multi_bots.sh
```

### 4. 查看状态

```bash
bash status_multi_bots.sh
```

### 5. 停止所有 Bot

```bash
bash stop_multi_bots.sh
```

## 添加更多 Bot

### 方法1：使用脚本（推荐）

```bash
bash create_bot_config.sh
```

### 方法2：手动创建

1. 复制现有配置：
```bash
cp -r bots/bot1 bots/bot2
```

2. 修改 `bots/bot2/.env`：
```env
BOT_ID=bot2
BOT_QQ=3456789012
PORT=8081
NAPCAT_PORT=3001
```

3. 重启：
```bash
bash start_multi_bots.sh
```

## 目录结构

```
zhinai-bot-v3/
├── bots/                           # Bot 配置目录
│   ├── bot1/                       # Bot1 配置
│   │   ├── .env                    # 环境变量
│   │   └── napcat/                 # NapCat 数据
│   ├── bot2/                       # Bot2 配置
│   │   ├── .env
│   │   └── napcat/
│   └── bot3/                       # Bot3 配置
│       ├── .env
│       └── napcat/
├── data/                           # 共享数据库
│   ├── conversations/              # 对话向量库
│   └── knowledge/                  # 知识向量库
├── logs/                           # 日志目录
│   ├── bot1.log                    # Bot1 日志
│   ├── bot1_napcat.log             # Bot1 NapCat 日志
│   ├── bot2.log
│   └── bot2_napcat.log
├── pids/                           # 进程 ID 文件
│   ├── bot1_nonebot.pid
│   ├── bot1_napcat.pid
│   ├── bot2_nonebot.pid
│   └── bot2_napcat.pid
├── start_multi_bots.sh             # 启动脚本
├── stop_multi_bots.sh              # 停止脚本
├── status_multi_bots.sh            # 状态查看脚本
└── create_bot_config.sh            # 配置创建脚本
```

## 端口分配规则

| Bot | NoneBot 端口 | NapCat 端口 |
|-----|-------------|-------------|
| bot1 | 8080 | 3000 |
| bot2 | 8081 | 3001 |
| bot3 | 8082 | 3002 |
| bot4 | 8083 | 3003 |
| ... | ... | ... |

## 数据隔离

虽然所有 Bot 共享同一个数据库，但数据是按 `user_id` 自动隔离的：

- 用户 A 和 Bot1 的对话 → 存储为 user_id=A
- 用户 A 和 Bot2 的对话 → 存储为 user_id=A
- 用户 B 和 Bot1 的对话 → 存储为 user_id=B

**结果**：
- 用户 A 在任何 Bot 上都能访问自己的历史记录
- 用户 A 看不到用户 B 的数据
- 不同 Bot 之间的数据互通（同一用户）

## 常见问题

### Q1: 端口被占用怎么办？

```bash
# 查看端口占用
lsof -i :8080

# 修改配置文件中的端口
vim bots/bot1/.env
# 修改 PORT=8080 为其他端口
```

### Q2: 如何查看日志？

```bash
# 实时查看 Bot1 日志
tail -f logs/bot1.log

# 查看最近 100 行
tail -100 logs/bot1.log

# 查看所有 Bot 日志
tail -f logs/*.log
```

### Q3: 如何重启单个 Bot？

```bash
# 停止 Bot1
kill $(cat pids/bot1_nonebot.pid)
kill $(cat pids/bot1_napcat.pid)

# 重新启动
bash start_multi_bots.sh
```

### Q4: 内存占用太高怎么办？

每个 Bot 约占用 200-500MB 内存。如果内存不足：

1. 减少 Bot 数量
2. 增加服务器内存
3. 使用更轻量的 LLM（如 DeepSeek-Lite）

### Q5: 如何备份数据？

```bash
# 备份数据库
tar -czf backup_$(date +%Y%m%d).tar.gz data/

# 备份配置
tar -czf config_backup_$(date +%Y%m%d).tar.gz bots/
```

## 监控和维护

### 1. 定期检查状态

```bash
# 添加到 crontab
*/5 * * * * cd /path/to/zhinai-bot-v3 && bash status_multi_bots.sh >> logs/monitor.log
```

### 2. 日志轮转

```bash
# 创建日志轮转配置
cat > /etc/logrotate.d/zhinai-bot << EOF
/path/to/zhinai-bot-v3/logs/*.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
}
EOF
```

### 3. 自动重启（可选）

```bash
# 创建守护脚本
cat > monitor_bots.sh << 'EOF'
#!/bin/bash
while true; do
    for pid_file in pids/*_nonebot.pid; do
        if [ -f "$pid_file" ]; then
            pid=$(cat "$pid_file")
            if ! ps -p $pid > /dev/null; then
                echo "Bot 崩溃，正在重启..."
                bash start_multi_bots.sh
            fi
        fi
    done
    sleep 60
done
EOF

chmod +x monitor_bots.sh
nohup ./monitor_bots.sh &
```

## 性能优化

### 1. 使用 Nginx 负载均衡（可选）

```nginx
upstream bots {
    server 127.0.0.1:8080;
    server 127.0.0.1:8081;
    server 127.0.0.1:8082;
}

server {
    listen 80;
    location / {
        proxy_pass http://bots;
    }
}
```

### 2. 数据库优化

- 定期清理旧对话（保留最近 3 个月）
- 使用 SSD 存储数据库
- 考虑迁移到 PostgreSQL（大规模部署）

### 3. 缓存优化

- 启用 Redis 缓存常用数据
- 缓存 LLM 响应（相同问题）

## 安全建议

1. **API 密钥保护**
   - 不要提交 .env 文件到 Git
   - 使用环境变量或密钥管理服务

2. **访问控制**
   - 配置防火墙规则
   - 只开放必要的端口

3. **数据加密**
   - 敏感数据加密存储
   - 使用 HTTPS 传输

4. **定期更新**
   - 及时更新依赖包
   - 关注安全漏洞

## 下一步

- [ ] 添加 Web 管理界面
- [ ] 实现自动扩容
- [ ] 添加性能监控
- [ ] 实现分布式部署
