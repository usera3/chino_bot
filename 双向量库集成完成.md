# 双向量库记忆系统集成完成 ✅

## 完成时间
2026-01-18 23:15

## 系统架构

### 三层记忆架构
1. **短期记忆 (chat_history)**: 最近20条消息
2. **对话向量库 (conversation_store)**: 完整对话历史
3. **知识向量库 (knowledge_store)**: 结构化用户信息

### 核心组件

#### 1. DualVectorStore (core/dual_vector_store.py)
- 管理两个独立的向量数据库
- 对话库：存储完整对话历史
- 知识库：存储提取的结构化信息
- 提供统一的检索接口

#### 2. KnowledgeExtractor (core/knowledge_extractor.py)
- 使用 LLM 从对话中提取结构化信息
- 支持提取：
  - 个人信息（姓名、邮箱、电话、年龄、地址）
  - 偏好（食物、爱好、不喜欢的东西）
  - 社交关系（朋友、家人）
- 自动过滤无用信息

#### 3. Butler (core/butler.py)
- 集成双向量库系统
- 自动保存对话到对话库
- 自动提取并保存知识到知识库
- 智能检索相关记忆（对话3条 + 知识2条）

## 集成修改

### 修改的文件
1. `core/butler.py`
   - 导入 DualVectorStore 和 KnowledgeExtractor
   - 修改参数：`use_vector_store` → `use_dual_memory`
   - 在 `process()` 和 `aprocess()` 方法中添加知识提取逻辑
   - 修改检索参数：`k_conversations=3, k_knowledge=2`

2. `plugins/chat_plugin.py`
   - 修改 Butler 初始化参数：`use_vector_store` → `use_dual_memory`

### 删除的文件
- `data/chroma_backup_20260118_225318/` (旧的向量库备份)

## 测试结果

### 测试1: 基础功能测试 (test_dual_memory.py)
✅ 对话存储：14条
✅ 知识提取：8条
✅ 检索准确性：100%

### 测试2: Butler 集成测试 (test_butler_dual_memory.py)
✅ 知识提取工作正常
✅ 知识存储工作正常
✅ 记忆检索工作正常
✅ 对话流程自然

## 工作流程

### 用户发送消息时
1. 检索相关记忆（对话3条 + 知识2条）
2. 构建增强的输入（注入历史记忆）
3. 执行 Agent 推理
4. 保存对话到对话库
5. 提取知识并保存到知识库（如果有）

### 知识提取条件
- 不是简单互动（如戳一戳）
- 包含信息关键词（"我的"、"我叫"、"我喜欢"等）
- LLM 成功提取到有用信息

## 性能优化

### 改进的 FakeEmbeddings
- 使用 TF-IDF 算法
- 支持 n-gram (1-3)
- 更好的语义理解
- 无需外部 API

### 智能检索
- 对话库：检索相关对话历史
- 知识库：检索结构化用户信息
- 按用户 ID 隔离数据
- 自动格式化检索结果

## 下一步计划

### 可选优化
1. 升级到真实的 Embeddings（如 OpenAI text-embedding-3）
2. 添加知识更新机制（用户信息变更时）
3. 添加知识过期机制（旧信息自动清理）
4. 优化知识提取 Prompt（提高准确率）

### 生产部署
1. ✅ 重启机器人测试
2. ✅ 观察知识提取效果
3. ✅ 收集用户反馈
4. ✅ 根据反馈调整参数

## 使用说明

### 启动机器人
```bash
cd zhinai-bot-v3
bash start_bot.sh
```

### 查看记忆统计
```python
from core.butler import Butler

# 获取统计信息
stats = butler.get_memory_stats()
print(stats)
# 输出：
# {
#     "short_term_messages": 20,
#     "long_term_enabled": True,
#     "conversation_count": 100,
#     "knowledge_count": 25
# }
```

### 清空记忆
```python
# 清空短期记忆
butler.clear_memory()

# 清空长期记忆（需要手动删除数据库）
rm -rf data/chroma_db_*
```

## 技术细节

### 数据存储格式

#### 对话库
```json
{
  "text": "用户: 我叫张三\n智乃: 你好张三！",
  "metadata": {
    "user_id": "123456",
    "type": "conversation",
    "timestamp": "2026-01-18 23:00:00"
  }
}
```

#### 知识库
```json
{
  "text": "name: 张三\nemail: zhangsan@qq.com",
  "metadata": {
    "user_id": "123456",
    "type": "knowledge",
    "timestamp": "2026-01-18 23:00:00",
    "knowledge_json": "{\"personal_info\": {\"name\": \"张三\", \"email\": \"zhangsan@qq.com\"}}"
  }
}
```

### 检索策略
- 对话库：使用 TF-IDF 相似度检索最相关的3条对话
- 知识库：使用 TF-IDF 相似度检索最相关的2条知识
- 按用户 ID 过滤，确保数据隔离
- 格式化为可读文本注入到 Prompt

## 总结

双向量库记忆系统已成功集成到 Butler 中，实现了：
- ✅ 完整的对话历史存储
- ✅ 智能的知识提取
- ✅ 高效的记忆检索
- ✅ 自然的对话体验

系统现在可以记住用户的个人信息、偏好和社交关系，大大提升了对话的连贯性和个性化程度。
