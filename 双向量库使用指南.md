# 双向量库记忆系统使用指南

## 快速开始

### 1. 启动机器人
```bash
cd zhinai-bot-v3
bash start_bot.sh
```

### 2. 测试对话
与机器人对话，告诉它一些个人信息：
- "我叫张三"
- "我的邮箱是 zhangsan@qq.com"
- "我喜欢吃火锅"
- "我住在北京"

### 3. 测试记忆
稍后询问机器人：
- "我叫什么名字？"
- "我的邮箱是什么？"
- "我喜欢吃什么？"

机器人应该能够准确回答这些问题！

## 系统特性

### 自动知识提取
机器人会自动从对话中提取以下信息：

#### 个人信息
- 姓名："我叫张三"
- 邮箱："我的邮箱是 zhangsan@qq.com"
- 电话："我的手机是 13800138000"
- 年龄："我今年25岁"
- 地址："我住在北京朝阳区"

#### 偏好信息
- 喜欢的食物："我喜欢吃火锅"
- 兴趣爱好："我喜欢打篮球"
- 不喜欢的东西："我不喜欢吃香菜"

#### 社交关系
- 朋友："小明是我的朋友"
- 家人："我爸爸是老师"

### 智能检索
当你提问时，机器人会：
1. 从知识库检索2条最相关的结构化信息
2. 从对话库检索3条最相关的历史对话
3. 结合短期记忆（最近20条消息）
4. 生成准确的回答

## 日志说明

### 正常日志
```
📚 检索到相关历史记忆:
【用户信息】
name: 张三
email: zhangsan@qq.com

【相关对话】
[1] 2026-01-18 23:00:00
用户: 我叫张三
智乃: 你好张三！
```

### 知识提取日志
```
💾 已保存知识: name: 张三...
💾 已保存到长期记忆（对话 + 知识）
```

### 无知识提取日志
```
💾 已保存到长期记忆（对话）
```

## 常见问题

### Q1: 机器人记不住我说的话？
**A:** 检查以下几点：
1. 确认 `use_dual_memory=True` 已启用
2. 查看日志是否有 "💾 已保存到长期记忆" 提示
3. 确认你的表述包含关键词（"我的"、"我叫"、"我喜欢"等）

### Q2: 知识提取不准确？
**A:** 可能的原因：
1. 表述不够明确（建议使用完整句子）
2. LLM 理解错误（可以调整 extract_prompt）
3. 信息太复杂（建议分多次对话）

### Q3: 如何清空记忆？
**A:** 
```bash
# 清空所有记忆（包括对话和知识）
rm -rf zhinai-bot-v3/data/chroma_db_*

# 重启机器人
bash start_bot.sh
```

### Q4: 如何查看存储的知识？
**A:** 使用测试脚本：
```bash
cd zhinai-bot-v3
python test_dual_memory.py
```

### Q5: 如何升级到真实的 Embeddings？
**A:** 修改 `plugins/chat_plugin.py`：
```python
butler = Butler(
    llm=llm,
    tools=tools,
    verbose=True,
    use_dual_memory=True,
    embedding_type="openai"  # 改为 "openai"
)
```
需要设置环境变量：
```bash
export OPENAI_API_KEY="your-api-key"
```

## 性能优化建议

### 1. 调整检索数量
在 `core/butler.py` 中修改：
```python
relevant_context = self.dual_store.get_relevant_context(
    query=user_input,
    user_id=user_id,
    k_conversations=5,  # 增加对话检索数量
    k_knowledge=3       # 增加知识检索数量
)
```

### 2. 调整短期记忆大小
在 `core/butler.py` 中修改：
```python
# 正常对话：保留最近15轮对话（30条消息）
if len(self.chat_history) > 30:
    self.chat_history = self.chat_history[-30:]
```

### 3. 优化知识提取 Prompt
修改 `core/knowledge_extractor.py` 中的 `extract_prompt`，添加更多示例或调整格式。

## 监控和调试

### 查看记忆统计
```python
from core.butler import Butler

stats = butler.get_memory_stats()
print(f"短期记忆: {stats['short_term_messages']} 条")
print(f"对话历史: {stats['conversation_count']} 条")
print(f"知识条目: {stats['knowledge_count']} 条")
```

### 启用详细日志
在 `plugins/chat_plugin.py` 中：
```python
butler = Butler(
    llm=llm,
    tools=tools,
    verbose=True,  # 确保为 True
    use_dual_memory=True,
    embedding_type="fake"
)
```

### 测试记忆检索
```python
from core.dual_vector_store import DualVectorStore

store = DualVectorStore(persist_directory="./data", embedding_type="fake")
context = store.get_relevant_context(
    query="我的邮箱",
    user_id="your_qq_id",
    k_conversations=3,
    k_knowledge=2
)
print(context)
```

## 最佳实践

### 1. 信息输入
- ✅ 使用完整句子："我叫张三"
- ❌ 避免简短回答："张三"

### 2. 信息更新
- 如果信息变更，直接告诉机器人新信息
- 系统会自动添加新的知识条目
- 旧信息仍会保留（可以手动清理）

### 3. 隐私保护
- 所有数据按用户 ID 隔离
- 不同用户的数据互不干扰
- 可以随时删除个人数据

### 4. 定期维护
- 建议每月备份一次数据库
- 定期检查知识提取准确性
- 根据用户反馈调整参数

## 技术支持

如有问题，请查看：
1. `双向量库架构设计.md` - 系统架构说明
2. `双向量库集成完成.md` - 集成详情
3. `test_dual_memory.py` - 测试示例
4. `test_butler_dual_memory.py` - Butler 集成测试

或查看日志文件：
```bash
tail -f logs/bot.log
```
