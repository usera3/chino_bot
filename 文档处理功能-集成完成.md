# 文档处理功能 - 集成完成

## ✅ 安装状态

**已成功集成文档处理功能到 zhinai-bot-v3！**

- ✅ 不依赖 Kiro
- ✅ 不依赖 MCP
- ✅ 直接使用 Python 库
- ✅ 作为 LangChain Tool 集成
- ✅ 即使 Kiro 关闭也能正常工作

## 已安装的 Python 库

```bash
✅ python-docx  # Word 文档处理
✅ PyPDF2       # PDF 文档处理
✅ openpyxl     # Excel 文档处理
✅ pandas       # 数据处理
✅ pypandoc     # 文档格式转换
✅ markdown     # Markdown 处理
```

## 可用工具（共 5 个）

### 1. read_pdf - 读取 PDF 文件

**功能**：提取 PDF 文件的文本内容

**参数**：
- `file_path`: PDF 文件路径（必需）
- `max_pages`: 最多读取的页数（可选）

**使用示例**：
```
用户：@智乃 读一下这个 PDF
[用户发送 PDF 文件]

机器人：
📄 PDF 文档信息
📁 文件：报告.pdf
📊 总页数：10
📖 已读取：10 页
==================================================

[第 1 页]
这是一份关于...的报告...
```

### 2. read_word - 读取 Word 文档

**功能**：提取 Word 文档的文本内容

**参数**：
- `file_path`: Word 文件路径（必需，.docx 格式）

**使用示例**：
```
用户：@智乃 这个 Word 文档写了什么？
[用户发送 Word 文件]

机器人：
📝 Word 文档信息
📁 文件：会议记录.docx
📊 段落数：15
==================================================

会议主题：项目进度讨论
...
```

### 3. create_word - 创建 Word 文档

**功能**：创建一个新的 Word 文档

**参数**：
- `file_path`: 保存的文件路径（必需）
- `title`: 文档标题（必需）
- `content`: 文档内容（必需，多段落用 \n\n 分隔）

**使用示例**：
```
用户：@智乃 帮我创建一个 Word 文档，标题是"会议记录"，内容是"今天讨论了项目进度"

机器人：
✅ Word 文档创建成功！
📁 文件：/path/to/会议记录.docx
📊 大小：12.5 KB
📝 段落数：1
```

### 4. read_excel - 读取 Excel 表格

**功能**：读取 Excel 表格的内容

**参数**：
- `file_path`: Excel 文件路径（必需）
- `sheet_name`: 工作表名称（可选）
- `max_rows`: 最多读取的行数（可选，默认 100）

**使用示例**：
```
用户：@智乃 这个 Excel 有什么数据？
[用户发送 Excel 文件]

机器人：
📊 Excel 表格信息
📁 文件：数据表.xlsx
📋 工作表：Sheet1, Sheet2
📏 当前表：Sheet1
📊 行数：50 / 列数：5
==================================================

姓名    年龄  职位      部门      工资
张三    25   工程师    技术部    8000
李四    30   经理      销售部    12000
...
```

### 5. create_excel - 创建 Excel 表格

**功能**：创建一个新的 Excel 表格

**参数**：
- `file_path`: 保存的文件路径（必需）
- `data`: 表格数据（必需，JSON 格式字符串）
- `sheet_name`: 工作表名称（可选，默认 Sheet1）

**使用示例**：
```
用户：@智乃 创建一个 Excel 表格，第一行是"姓名、年龄、职位"，第二行是"张三、25、工程师"

机器人：
✅ Excel 表格创建成功！
📁 文件：/path/to/数据表.xlsx
📊 大小：8.3 KB
📏 行数：1 / 列数：3
```

## 工具统计

机器人现在共有 **24 个工具**：

1. 基础工具（5个）：时间、天气、情感、邮件
2. LangChain 工具（3个）：搜索、维基百科、Python REPL
3. 图像识别工具（1个）：Qwen-VL
4. 定时任务工具（1个）
5. 链接解析工具（1个）
6. HTML 渲染工具（1个）
7. 图片搜索工具（1个）
8. 文件传输工具（2个）：上传群文件、获取群文件列表
9. **文档处理工具（5个）**：读 PDF、读 Word、创建 Word、读 Excel、创建 Excel
10. 工作流工具（1个）
11. QQ 互动工具（3个）：点赞、获取用户信息、获取群成员信息

## 完整工作流

### 工作流 1：接收文档 → 阅读 → 回答问题

```
1. 用户发送 PDF 文件到群里
2. 用户：@智乃 这个 PDF 讲的是什么？
3. 机器人：
   - 接收文件消息
   - 下载 PDF 到本地
   - 调用 read_pdf 工具
   - 提取文本内容
   - 理解并总结
   - 回答用户问题
```

### 工作流 2：创建文档 → 上传群文件

```
1. 用户：@智乃 创建一个 Word 文档，标题是"会议记录"，内容是"今天讨论了项目进度"
2. 机器人：
   - 调用 create_word 工具
   - 创建 Word 文档
   - 调用 upload_group_file 工具
   - 上传到群文件
   - 通知用户完成
```

### 工作流 3：读取群文件 → 处理 → 回复

```
1. 用户：@智乃 群文件里有个报告.pdf，帮我总结一下
2. 机器人：
   - 调用 get_group_files 查找文件
   - 下载 PDF 文件
   - 调用 read_pdf 工具
   - 提取并总结内容
   - 回复用户
```

### 工作流 4：数据处理 → 创建表格 → 分享

```
1. 用户：@智乃 把这些数据整理成 Excel 表格：张三 25 工程师，李四 30 经理
2. 机器人：
   - 解析用户提供的数据
   - 构造 JSON 格式
   - 调用 create_excel 工具
   - 创建 Excel 文件
   - 上传到群文件
   - 通知用户完成
```

## 智能推理能力

机器人会自动推理用户的意图：

### 示例 1：隐式读取

```
用户：@智乃 这个文档说了什么？
[用户发送 PDF]

机器人推理：
1. 识别用户发送了 PDF 文件
2. 推理用户想要了解文件内容
3. 自动下载文件
4. 调用 read_pdf 工具
5. 总结并回答
```

### 示例 2：格式推理

```
用户：@智乃 帮我读一下这个文件
[用户发送 .docx 文件]

机器人推理：
1. 识别文件格式是 .docx
2. 推理应该使用 read_word 工具
3. 自动调用正确的工具
4. 提取并回答
```

### 示例 3：创建推理

```
用户：@智乃 把这些内容保存成文档：标题是"会议记录"，内容是...

机器人推理：
1. 识别用户想要创建文档
2. 提取标题和内容
3. 推理文件路径（自动生成）
4. 调用 create_word 工具
5. 上传到群文件
```

## 与其他功能的结合

### 1. 文件传输 + 文档处理

```
完整流程：
1. 用户发送文件 → 机器人接收
2. 机器人读取文件 → 提取内容
3. 机器人处理内容 → 生成新文件
4. 机器人上传文件 → 分享给用户
```

### 2. 图像识别 + 文档处理

```
用户：@智乃 这张图片上的文字是什么？把它保存成 Word
[用户发送图片]

机器人：
1. 使用图像识别工具识别文字
2. 调用 create_word 工具创建文档
3. 上传到群文件
```

### 3. 定时任务 + 文档处理

```
用户：@智乃 每天早上9点把昨天的聊天记录整理成 Word 文档

机器人：
1. 创建定时任务
2. 每天9点执行：
   - 提取聊天记录
   - 调用 create_word 工具
   - 上传到群文件
```

### 4. 搜索 + 文档处理

```
用户：@智乃 搜索一下 AI 的最新进展，然后整理成 Word 文档

机器人：
1. 调用搜索工具
2. 获取搜索结果
3. 整理内容
4. 调用 create_word 工具
5. 上传到群文件
```

## 技术实现

### 1. 工具定义

在 `tools/document_tools.py` 中定义了 5 个 LangChain Tool：
- `ReadPDFTool`
- `ReadWordTool`
- `CreateWordTool`
- `ReadExcelTool`
- `CreateExcelTool`

### 2. 依赖库

```python
from PyPDF2 import PdfReader          # PDF 处理
from docx import Document              # Word 处理
import pandas as pd                    # Excel 处理
import json                            # 数据解析
```

### 3. 集成方式

```python
# 在 tools/basic_tools.py 中
from .document_tools import get_all_document_tools

doc_tools = get_all_document_tools()
all_tools.extend(doc_tools)
```

### 4. 自动推理

通过 Butler 的智能推理能力，机器人会：
- 自动识别文件格式
- 自动选择合适的工具
- 自动生成文件路径
- 自动上传到群文件

## 注意事项

1. **文件路径**
   - 文件必须在本地文件系统上
   - 建议使用绝对路径
   - 自动创建不存在的目录

2. **文件大小**
   - PDF：建议不超过 50MB
   - Word：建议不超过 20MB
   - Excel：建议不超过 10MB

3. **格式支持**
   - Word：仅支持 .docx 格式（不支持 .doc）
   - Excel：支持 .xlsx 和 .xls
   - PDF：支持文本 PDF（扫描版需要 OCR）

4. **性能**
   - 大文件处理可能需要较长时间
   - 建议设置合理的超时时间

## 测试建议

### 测试 1：读取 PDF

1. 准备一个 PDF 文件
2. 发送到群里
3. @智乃 这个 PDF 讲的是什么？
4. 观察机器人是否能正确读取并总结

### 测试 2：创建 Word

1. @智乃 创建一个 Word 文档，标题是"测试"，内容是"这是测试内容"
2. 观察机器人是否能正确创建
3. 检查文件是否上传到群文件

### 测试 3：读取 Excel

1. 准备一个 Excel 文件
2. 发送到群里
3. @智乃 这个表格有什么数据？
4. 观察机器人是否能正确读取并显示

### 测试 4：创建 Excel

1. @智乃 创建一个 Excel 表格，包含姓名和年龄两列
2. 观察机器人是否能正确创建
3. 检查文件内容是否正确

## 故障排除

### 问题 1：无法读取 PDF

**可能原因**：
- PDF 是扫描版（图片 PDF）
- PDF 加密或损坏

**解决方案**：
- 使用文本 PDF
- 如需 OCR，可以添加 OCR 工具

### 问题 2：Word 创建失败

**可能原因**：
- 文件路径不存在
- 权限不足

**解决方案**：
- 检查文件路径
- 确保有写入权限

### 问题 3：Excel 数据格式错误

**可能原因**：
- JSON 格式不正确
- 数据类型不匹配

**解决方案**：
- 检查 JSON 格式
- 确保数据类型正确

## 下一步扩展

1. **PDF 创建**
   - 添加 PDF 创建工具
   - 支持 Word 转 PDF

2. **OCR 功能**
   - 添加 OCR 工具
   - 支持扫描版 PDF

3. **格式转换**
   - PDF ↔ Word
   - Excel ↔ CSV
   - Markdown ↔ Word

4. **高级编辑**
   - Word 段落编辑
   - Excel 单元格编辑
   - PDF 合并分割

## 相关文档

- `文件传输功能说明.md` - 文件传输功能
- `智能推理能力设计.md` - 智能推理能力
- `tools/document_tools.py` - 文档处理工具源码

## 总结

✅ 文档处理功能已成功集成到 zhinai-bot-v3
✅ 不依赖 Kiro 和 MCP，完全独立运行
✅ 支持 PDF、Word、Excel 的读取和创建
✅ 与文件传输功能完美结合
✅ 具备智能推理能力
✅ 共 24 个工具，功能完整

机器人现在是一个完整的文档处理助手！🎉
