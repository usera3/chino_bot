# 混合记忆方案设计

## 🎯 问题分析

### 当前困境

1. **只用 chat_history**
   - ✅ 效果好（完整上下文）
   - ❌ 超过 5-10 轮就爆 token
   - ❌ 成本高

2. **只用向量检索**
   - ✅ 可扩展（无限历史）
   - ❌ 检索不准确
   - ❌ 丢失对话连贯性

### 根本原因

**向量检索的局限性**：
- 语义相似 ≠ 对话相关
- 例如："我的邮箱是xxx" 和 "发邮件" 语义不够相似
- FakeEmbeddings 更是雪上加霜

## 💡 混合方案

### 架构设计

```python
┌─────────────────────────────────────────────────┐
│              用户输入                            │
└─────────────────────────────────────────────────┘
                    ↓
    ┌───────────────┴───────────────┐
    ↓                               ↓
┌─────────────┐              ┌──────────────┐
│ 短期记忆     │              │ 长期记忆      │
│ (最近10轮)   │              │ (向量检索)    │
│ - 完整对话   │              │ - 关键信息    │
│ - 保持连贯   │              │ - 用户偏好    │
└─────────────┘              └──────────────┘
    ↓                               ↓
    └───────────────┬───────────────┘
                    ↓
         ┌──────────────────┐
         │  智能合并         │
         │  - 去重           │
         │  - 排序           │
         │  - 压缩           │
         └──────────────────┘
                    ↓
         ┌──────────────────┐
         │  LLM 生成回复     │
         └──────────────────┘
```

### 关键策略

#### 1. 分类存储

**短期记忆**（chat_history）：
- 最近 10 轮对话
- 完整保留，不压缩
- 用于保持对话连贯性

**长期记忆**（向量数据库）：
- 所有历史对话
- 但**只检索关键信息**
- 用于记住用户偏好、重要事实

#### 2. 智能检索

**不是所有消息都检索向量库**，而是：

```python
# 触发长期记忆检索的条件
if any([
    "记得" in user_input,
    "还记得" in user_input,
    "之前" in user_input,
    "上次" in user_input,
    "我的" in user_input,  # "我的邮箱"、"我的名字"
    "告诉过你" in user_input,
    # ... 更多关键词
]):
    # 检索长期记忆
    relevant_context = vector_store.search(...)
```

#### 3. 结构化存储

**不只存对话，还存结构化信息**：

```python
# 传统方式（效果差）
"用户: 我的邮箱是 1446437177@qq.com"

# 改进方式（效果好）
{
    "type": "user_info",
    "key": "email",
    "value": "1446437177@qq.com",
    "user_id": "1446437177",
    "timestamp": "2026-01-18 22:00:00",
    "context": "用户告诉我他的邮箱是 1446437177@qq.com"
}
```

#### 4. 对话摘要

**超过 10 轮后，自动生成摘要**：

```python
# 原始对话（10轮，2000 tokens）
用户: 我叫张三
AI: 你好张三
用户: 我的邮箱是 xxx@qq.com
AI: 记住了
...

# 压缩为摘要（100 tokens）
"""
用户信息：
- 姓名：张三
- 邮箱：xxx@qq.com
- 偏好：喜欢吃火锅
- 最近话题：讨论了工作和天气
"""
```

## 🔧 实现方案

### 方案 A：渐进式优化（推荐）

**第一步：增加短期记忆容量**
```python
# 当前：最近 2-3 轮（4-6 条消息）
# 改为：最近 10 轮（20 条消息）

if len(self.chat_history) > 20:
    self.chat_history = self.chat_history[-20:]
```

**第二步：智能触发长期记忆**
```python
# 只在需要时检索
keywords = ["记得", "还记得", "之前", "我的", "告诉过"]
if any(kw in user_input for kw in keywords):
    relevant_context = vector_store.search(...)
```

**第三步：结构化信息提取**
```python
# 使用 LLM 提取关键信息
if "我的" in user_input or "是" in user_input:
    extracted_info = llm.extract_info(user_input)
    # 存储结构化数据
    db.save_user_info(extracted_info)
```

### 方案 B：完整混合架构

**实现三层记忆**：

```python
class HybridMemory:
    def __init__(self):
        self.short_term = []  # 最近 10 轮
        self.structured_info = {}  # 结构化信息
        self.vector_store = VectorStore()  # 向量检索
    
    def add_message(self, user_input, bot_response):
        # 1. 添加到短期记忆
        self.short_term.append((user_input, bot_response))
        if len(self.short_term) > 10:
            # 生成摘要
            summary = self.summarize(self.short_term[:5])
            # 存入向量库
            self.vector_store.add(summary)
            # 清理短期记忆
            self.short_term = self.short_term[5:]
        
        # 2. 提取结构化信息
        info = self.extract_info(user_input)
        if info:
            self.structured_info.update(info)
        
        # 3. 存入向量库
        self.vector_store.add(user_input, bot_response)
    
    def get_context(self, user_input):
        # 1. 短期记忆（总是包含）
        context = self.format_short_term()
        
        # 2. 结构化信息（如果相关）
        if self.is_asking_about_info(user_input):
            context += self.format_structured_info()
        
        # 3. 向量检索（如果需要）
        if self.needs_long_term_memory(user_input):
            context += self.vector_store.search(user_input)
        
        return context
```

## 📊 效果对比

| 方案 | Token 消耗 | 准确率 | 实现难度 |
|------|-----------|--------|---------|
| 纯 chat_history | 高（指数增长） | 95% | 简单 |
| 纯向量检索 | 低（固定） | 60% | 简单 |
| **混合方案** | **中（线性增长）** | **85%** | **中等** |
| 混合+摘要 | 低（固定） | 90% | 复杂 |

## 🎯 快速实现

### 最小改动方案

只需修改 `core/butler.py`：

```python
# 1. 增加短期记忆容量
if len(self.chat_history) > 20:  # 改为 20
    self.chat_history = self.chat_history[-20:]

# 2. 智能触发长期记忆
memory_keywords = ["记得", "还记得", "之前", "上次", "我的", "告诉过"]
needs_long_term = any(kw in user_input for kw in memory_keywords)

if needs_long_term and self.use_vector_store:
    relevant_context = self.vector_store.get_relevant_context(...)
```

### 预期效果

- ✅ 短期对话（10轮内）：完美记忆
- ✅ 关键信息（邮箱、偏好）：主动检索
- ✅ Token 消耗：可控（最多 20 条消息）
- ✅ 实现成本：低（只改几行代码）

## 🚀 下一步

1. **立即实施**：增加短期记忆到 20 条
2. **测试效果**：看是否解决问题
3. **如果不够**：添加智能触发逻辑
4. **长期优化**：实现对话摘要

要不要我现在就实现这个方案？
