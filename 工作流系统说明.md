# 工作流系统说明

## 概述

工作流系统是一个强大的任务调度和编排框架，允许 AI 创建复杂的多步骤任务流程，在指定时间自动执行。

## 核心特性

### 1. 多步骤执行
- 支持按顺序执行多个工具调用
- 每个步骤可以调用不同的工具
- 步骤间可以传递数据

### 2. 数据传递
- 使用 `output_key` 保存步骤输出
- 使用 `{{variable}}` 模板语法引用前面步骤的输出
- 支持嵌套数据结构

### 3. 时间调度
- **相对时间**：`10秒后`、`5分钟后`、`2小时后`
- **绝对时间**：`12:24`、`14:30:00`
- 自动处理时区（东八区）

### 4. 错误处理
- **continue**：步骤失败后继续执行后续步骤
- **stop**：步骤失败后停止整个工作流
- 支持自动重试（默认 3 次）

### 5. 依赖管理
- 使用 `depends_on` 指定步骤依赖关系
- 确保依赖步骤先执行
- 支持复杂的依赖图

## 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    WorkflowTool                         │
│  (AI 创建工作流的接口)                                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                WorkflowScheduler                        │
│  (调度器 - 在指定时间触发工作流)                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                WorkflowExecutor                         │
│  (执行器 - 执行工作流中的所有步骤)                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   Tool Registry                         │
│  (工具注册表 - 所有可用的工具)                            │
└─────────────────────────────────────────────────────────┘
```

## 使用示例

### 示例 1：单步骤 - 定时发邮件

**用户请求**：`12:24 给我发邮件提醒吃饭`

**AI 生成的工作流**：
```json
{
    "name": "吃饭提醒",
    "trigger_time": "12:24",
    "steps": [
        {
            "tool_name": "send_email",
            "parameters": {
                "receiver_email": "123456@qq.com",
                "subject": "吃饭提醒",
                "content": "该吃饭啦！记得按时吃饭哦~"
            }
        }
    ]
}
```

### 示例 2：多步骤 - 先查天气再发邮件

**用户请求**：`10秒后查深圳天气然后发邮件告诉我`

**AI 生成的工作流**：
```json
{
    "name": "天气查询并通知",
    "trigger_time": "10秒后",
    "steps": [
        {
            "tool_name": "get_weather",
            "parameters": {"city": "深圳"},
            "output_key": "weather_info"
        },
        {
            "tool_name": "send_email",
            "parameters": {
                "receiver_email": "123456@qq.com",
                "subject": "深圳天气",
                "content": "深圳天气情况：{{weather_info}}"
            },
            "depends_on": [1]
        }
    ]
}
```

### 示例 3：复杂工作流 - 搜索、总结、通知

**用户请求**：`明天8点搜索AI新闻，然后发邮件总结给我`

**AI 生成的工作流**：
```json
{
    "name": "AI新闻总结",
    "trigger_time": "08:00",
    "steps": [
        {
            "tool_name": "search",
            "parameters": {"query": "AI 人工智能 最新新闻"},
            "output_key": "search_results"
        },
        {
            "tool_name": "send_email",
            "parameters": {
                "receiver_email": "123456@qq.com",
                "subject": "今日AI新闻总结",
                "content": "今日AI新闻：\n\n{{search_results}}"
            },
            "depends_on": [1]
        }
    ]
}
```

## 与旧版 schedule_task 的对比

### 旧版 schedule_task（已保留兼容）
- ✅ 简单易用
- ❌ 只支持单步骤
- ❌ 需要手动指定 action 类型
- ❌ 不支持数据传递

### 新版 create_workflow（推荐）
- ✅ 支持多步骤
- ✅ 自动识别工具类型
- ✅ 支持步骤间数据传递
- ✅ 更灵活的错误处理
- ✅ 支持复杂的依赖关系

## AI 使用指南

### 何时使用工作流？

1. **单步骤定时任务**
   - 用户：`10秒后提醒我`
   - 推荐：`create_workflow`（单步骤工作流）
   - 备选：`schedule_task`（兼容旧版）

2. **多步骤任务**
   - 用户：`10秒后查天气然后发邮件`
   - 必须：`create_workflow`（多步骤工作流）

3. **需要数据传递**
   - 用户：`搜索xxx然后把结果发给我`
   - 必须：`create_workflow`（使用模板变量）

### AI 的职责

1. **解析用户意图**
   - 识别需要哪些工具
   - 确定执行顺序
   - 识别数据依赖关系

2. **生成完整参数**
   - 为每个工具生成所有必需参数
   - 例如邮件的主题、内容等
   - 不要让用户补充参数

3. **设计数据流**
   - 使用 `output_key` 保存中间结果
   - 使用 `{{variable}}` 引用前面的输出
   - 确保数据流合理

## 技术细节

### 文件结构
```
zhinai-bot-v3/
├── models/
│   └── workflow_models.py      # 数据模型
├── core/
│   ├── workflow_scheduler.py   # 调度器
│   └── workflow_executor.py    # 执行器
└── tools/
    └── workflow_tool.py        # AI 接口
```

### 数据模型

#### WorkflowDefinition
- `workflow_id`: 唯一标识
- `name`: 工作流名称
- `user_id`: 创建者 QQ 号
- `trigger_time`: 触发时间（原始字符串）
- `trigger_timestamp`: 触发时间戳
- `steps`: 步骤列表
- `status`: 状态（pending/running/completed/failed/cancelled）
- `on_error`: 错误策略（continue/stop）
- `max_retries`: 最大重试次数

#### WorkflowStep
- `step_id`: 步骤 ID
- `tool_name`: 工具名称
- `parameters`: 参数（可包含模板变量）
- `output_key`: 输出键名（可选）
- `depends_on`: 依赖的步骤 ID 列表
- `is_critical`: 是否关键步骤
- `status`: 状态（pending/running/completed/failed）

### 执行流程

1. **创建阶段**
   - AI 调用 `create_workflow` 工具
   - 验证步骤定义（工具是否存在、依赖是否合法）
   - 解析触发时间
   - 创建 WorkflowDefinition 对象

2. **调度阶段**
   - WorkflowScheduler 注册工作流
   - 计算延迟时间
   - 创建异步任务等待触发

3. **执行阶段**
   - WorkflowExecutor 按顺序执行步骤
   - 解析模板变量（替换 `{{variable}}`）
   - 调用工具并收集输出
   - 处理错误和重试

4. **清理阶段**
   - 工作流完成后自动清理
   - 释放资源

## 最佳实践

### 1. 合理设计步骤
- 每个步骤只做一件事
- 避免过于复杂的工作流
- 考虑失败场景

### 2. 使用有意义的 output_key
- 使用描述性的键名（如 `weather_info`、`search_results`）
- 避免使用 `result1`、`result2` 等无意义的名称

### 3. 设置合理的错误策略
- 关键步骤：设置 `is_critical=true`
- 可选步骤：使用 `on_error="continue"`
- 重要工作流：使用 `on_error="stop"`

### 4. 提供友好的反馈
- 工作流创建成功后，清晰地告诉用户
- 说明触发时间和步骤
- 让用户知道会发生什么

## 未来扩展

### 计划中的功能
- [ ] 工作流持久化（保存到数据库）
- [ ] 工作流取消和修改
- [ ] 工作流执行历史查询
- [ ] 条件分支（if-else）
- [ ] 循环执行（for/while）
- [ ] 并行执行（parallel steps）
- [ ] 工作流模板（预定义常用工作流）

## 总结

工作流系统是 zhinai-bot-v3 的核心功能之一，它让 AI 能够创建和管理复杂的任务流程。通过合理使用工作流，可以实现：

- ✅ 定时任务自动化
- ✅ 多步骤任务编排
- ✅ 工具间数据传递
- ✅ 灵活的错误处理
- ✅ 可扩展的任务系统

这是一个可持续发展的解决方案，比简单地在 `schedule_task` 中添加更多 action 类型要优雅得多！
