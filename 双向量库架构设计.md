# 双向量库架构设计

## 🎯 设计目标

解决长期记忆的核心问题：
1. **短期记忆不够**：只能记住最近几轮对话
2. **向量检索不准**：语义相似 ≠ 对话相关
3. **信息提取困难**：关键信息淹没在对话中

## 🏗️ 三层记忆架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户输入                              │
└─────────────────────────────────────────────────────────┘
                         ↓
    ┌────────────────────┼────────────────────┐
    ↓                    ↓                    ↓
┌─────────┐      ┌──────────────┐      ┌──────────────┐
│ 短期记忆 │      │ 对话向量库    │      │ 知识向量库    │
│         │      │              │      │              │
│ 最近20条 │      │ 完整对话历史  │      │ 结构化信息    │
│ 消息     │      │              │      │              │
│         │      │ - 所有对话    │      │ - 姓名       │
│ 保持连贯 │      │ - 按时间排序  │      │ - 邮箱       │
│         │      │ - 语义检索    │      │ - 偏好       │
│         │      │              │      │ - 社交关系    │
└─────────┘      └──────────────┘      └──────────────┘
    ↓                    ↓                    ↓
    └────────────────────┼────────────────────┘
                         ↓
              ┌──────────────────┐
              │  智能合并         │
              │  - 去重           │
              │  - 优先级排序     │
              │  - 格式化         │
              └──────────────────┘
                         ↓
              ┌──────────────────┐
              │  LLM 生成回复     │
              └──────────────────┘
```

## 📊 数据流

### 1. 对话输入

```python
用户: "我的邮箱是 zhangsan@qq.com"
智乃: "好的，记住你的邮箱了"
```

### 2. 三路存储

**A. 短期记忆（chat_history）**
```python
self.chat_history.append(HumanMessage("我的邮箱是 zhangsan@qq.com"))
self.chat_history.append(AIMessage("好的，记住你的邮箱了"))
# 保留最近 20 条
```

**B. 对话向量库（conversation_store）**
```python
conversation_text = """
[2026-01-18 22:00:00] 
用户: 我的邮箱是 zhangsan@qq.com
智乃: 好的，记住你的邮箱了
"""
conversation_store.add(conversation_text, metadata={
    "user_id": "1446437177",
    "timestamp": "2026-01-18 22:00:00",
    "type": "conversation"
})
```

**C. 知识向量库（knowledge_store）**
```python
# LLM 提取结构化信息
knowledge = {
    "personal_info": {
        "email": "zhangsan@qq.com"
    }
}

knowledge_text = "email: zhangsan@qq.com"
knowledge_store.add(knowledge_text, metadata={
    "user_id": "1446437177",
    "type": "knowledge",
    "knowledge_json": json.dumps(knowledge)
})
```

### 3. 检索合并

当用户问："你还记得我的邮箱吗？"

```python
# 1. 短期记忆（总是包含）
short_term = chat_history[-20:]  # 最近 20 条

# 2. 知识库检索（优先级高）
knowledge = knowledge_store.search("邮箱 email", k=2)
# 结果: "email: zhangsan@qq.com"

# 3. 对话库检索（补充上下文）
conversations = conversation_store.search("邮箱", k=3)
# 结果: 包含邮箱的历史对话

# 4. 合并
context = f"""
【用户信息】
email: zhangsan@qq.com

【相关对话】
[1] 2026-01-18 22:00:00
用户: 我的邮箱是 zhangsan@qq.com
智乃: 好的，记住你的邮箱了

【最近对话】
{short_term}
"""
```

## 🔑 核心组件

### 1. DualVectorStore（双向量库）

```python
class DualVectorStore:
    def __init__(self):
        # 对话库：完整对话历史
        self.conversation_store = Chroma(collection_name="conversations")
        
        # 知识库：结构化信息
        self.knowledge_store = Chroma(collection_name="knowledge")
    
    def add_conversation(self, user_input, bot_response, user_id):
        """添加对话到对话库"""
        pass
    
    def add_knowledge(self, knowledge, user_id):
        """添加知识到知识库"""
        pass
    
    def get_relevant_context(self, query, user_id):
        """获取相关上下文（对话 + 知识）"""
        pass
```

### 2. KnowledgeExtractor（知识提取器）

```python
class KnowledgeExtractor:
    def __init__(self, llm):
        self.llm = llm
    
    def extract_knowledge(self, user_input, bot_response, user_id):
        """从对话中提取结构化知识"""
        # 使用 LLM 提取
        prompt = """
        从以下对话中提取用户信息：
        用户: {user_input}
        智乃: {bot_response}
        
        输出 JSON 格式：
        {
            "personal_info": {...},
            "preferences": {...},
            "social": {...}
        }
        """
        return llm.invoke(prompt)
```

## 📋 知识结构

### JSON Schema

```json
{
    "user_id": "1446437177",
    "personal_info": {
        "name": "张三",
        "email": "zhangsan@qq.com",
        "phone": "13800138000",
        "age": 25,
        "location": "北京"
    },
    "preferences": {
        "food": ["火锅", "烧烤"],
        "hobbies": ["游戏", "音乐"],
        "dislikes": ["香菜"]
    },
    "social": {
        "friends": ["李四", "王五"],
        "family": ["爸爸", "妈妈"]
    },
    "facts": [
        "在互联网公司工作",
        "喜欢养猫"
    ]
}
```

### 存储格式

**对话库**：
```
[2026-01-18 22:00:00] 用户: 我的邮箱是 zhangsan@qq.com
智乃: 好的，记住你的邮箱了
```

**知识库**：
```
name: 张三 | email: zhangsan@qq.com | location: 北京 | food: 火锅, 烧烤 | hobbies: 游戏, 音乐
```

## 🎯 检索策略

### 优先级

1. **知识库**（最高优先级）
   - 结构化信息
   - 精确匹配
   - 用于回答"我的xxx是什么"

2. **对话库**（中等优先级）
   - 完整对话历史
   - 语义检索
   - 用于回忆"之前说过什么"

3. **短期记忆**（总是包含）
   - 最近 20 条消息
   - 保持对话连贯性

### 触发条件

**总是检索知识库**：
- 包含"我的"、"我是"、"我叫"
- 包含"记得"、"还记得"
- 包含"之前"、"上次"

**按需检索对话库**：
- 包含"说过"、"提到"
- 包含"那次"、"当时"

## 💾 存储位置

```
data/
├── conversations/          # 对话向量库
│   ├── chroma.sqlite3
│   └── ...
└── knowledge/             # 知识向量库
    ├── chroma.sqlite3
    └── ...
```

## 🔄 工作流程

### 对话处理流程

```python
def process_message(user_input, user_id):
    # 1. 添加到短期记忆
    chat_history.append(user_input)
    
    # 2. 检索相关上下文
    context = dual_vector_store.get_relevant_context(user_input, user_id)
    
    # 3. 合并上下文
    full_context = f"{context}\n\n【最近对话】\n{chat_history[-20:]}"
    
    # 4. 生成回复
    response = llm.generate(full_context + user_input)
    
    # 5. 保存对话
    dual_vector_store.add_conversation(user_input, response, user_id)
    
    # 6. 提取知识（异步）
    knowledge = extractor.extract_knowledge(user_input, response, user_id)
    if knowledge:
        dual_vector_store.add_knowledge(knowledge, user_id)
    
    return response
```

## 📊 性能优化

### Token 消耗

| 组件 | Token 消耗 | 说明 |
|------|-----------|------|
| 短期记忆 | ~2000 | 20 条消息 × 100 tokens |
| 知识检索 | ~200 | 2 条知识 × 100 tokens |
| 对话检索 | ~600 | 3 条对话 × 200 tokens |
| **总计** | **~2800** | 可控范围 |

### 优化策略

1. **知识提取异步化**
   - 不阻塞对话
   - 后台处理

2. **批量提取**
   - 每 10 条对话提取一次
   - 减少 LLM 调用

3. **缓存常用知识**
   - 用户基本信息缓存
   - 减少检索次数

## ✅ 优势

1. **准确性高**
   - 结构化存储，精确匹配
   - 不依赖语义相似度

2. **可扩展**
   - 无限对话历史
   - Token 消耗可控

3. **连贯性好**
   - 短期记忆保持对话流畅
   - 长期记忆提供背景信息

4. **灵活性强**
   - 可以按需检索
   - 支持多种查询方式

## 🚀 实施计划

### Phase 1：基础实现（当前）
- ✅ 创建 DualVectorStore
- ✅ 创建 KnowledgeExtractor
- ⬜ 集成到 Butler
- ⬜ 测试验证

### Phase 2：优化（下一步）
- ⬜ 异步知识提取
- ⬜ 批量处理
- ⬜ 缓存机制

### Phase 3：增强（未来）
- ⬜ 知识图谱
- ⬜ 关系推理
- ⬜ 主动提醒

## 📚 参考

- [企业级长期记忆解决方案](./企业级长期记忆解决方案.md)
- [混合记忆方案](./混合记忆方案.md)
- [长期记忆升级说明](./长期记忆升级说明.md)
