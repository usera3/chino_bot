# 智能推理能力设计

## 核心理念

**机器人不应该是一个"问答机器"，而应该是一个"推理助手"。**

用户说"给我发个邮件"时：
- ❌ 机械式：回复"你的邮箱是什么？"
- ✅ 智能式：推理 QQ 号 → 推理邮箱 → 发送邮件

这不仅仅是邮件的问题，而是**所有复杂问题的缩影**。

## 设计哲学

### 1. 主动推理，不被动询问

**原则**：能推理出来的信息，不要问用户。

**实现方式**：
- 充分利用上下文信息（QQ 号、群号、被 @ 的用户等）
- 利用工具串联（先获取信息，再使用信息）
- 利用常识推理（QQ 号 → QQ 邮箱）

**示例**：
```
用户："给我发个邮件"

机械式流程：
1. 回复："你的邮箱是什么？"
2. 等待用户回复
3. 发送邮件

智能式流程：
1. 从系统提示中获取用户 QQ 号：123456789
2. 推理邮箱地址：123456789@qq.com
3. 调用 send_email 工具
4. 回复："已经发到你的 QQ 邮箱啦~"
```

### 2. 多步骤思考

**原则**：复杂问题分解成多个步骤，每个步骤可能需要不同的工具。

**实现方式**：
- 识别任务的依赖关系
- 按顺序调用工具
- 工具之间传递信息

**示例**：
```
用户："查一下我的信息然后发邮件给我"

步骤分解：
1. 调用 get_user_info 工具
2. 从结果中提取 QQ 号
3. 推理邮箱地址
4. 调用 send_email 工具
```

### 3. 利用上下文信息

**原则**：系统会注入很多有用的信息，要充分利用。

**可用信息**：
- 用户的 QQ 号
- 群号（如果在群聊中）
- 被 @ 的用户列表
- 机器人自己的 QQ 号

**示例**：
```
系统提示：
[系统提示：发送此消息的用户是张三(QQ:123456789)，当前在群878812866中。
你的 QQ 号是 1000000000，用户的 QQ 号是 123456789]

用户："给我发个邮件"

推理过程：
1. 从系统提示中看到用户 QQ 号：123456789
2. 推理邮箱：123456789@qq.com
3. 发送邮件
```

### 4. 智能补全缺失信息

**原则**：用户没有提供的信息，尝试通过工具获取或推理。

**常见模式**：
- 缺少 QQ 号 → 从上下文获取或调用 get_user_info
- 缺少邮箱 → 从 QQ 号推理
- 缺少时间 → 调用 get_time
- 缺少天气 → 调用 get_weather

**示例**：
```
用户："给我点赞"

推理过程：
1. 检查上下文中是否有用户 QQ 号
2. 如果有，直接调用 send_like（工具会自动使用）
3. 如果没有，先调用 get_user_info 获取
```

### 5. 常见推理模式

**模式库**：
- `QQ 号 → QQ 邮箱`：`{qq_number}@qq.com`
- `用户说"我" → 当前对话用户`
- `用户说"给xxx" → 如果 xxx 被 @，使用被 @ 的用户`
- `时间相关 → get_time 工具`
- `天气相关 → get_weather 工具`
- `"画xxx" → render_html 工具`
- `"找xxx图片" → search_image 工具`

## 实现方式

### 1. 系统提示词设计

在 `core/butler.py` 中，系统提示词包含：

```python
## 🧠 核心推理能力（最重要）

你不是一个简单的问答机器人，而是一个**具有推理能力的智能助手**。

### 推理原则

1. **主动推理，不要被动询问**
2. **多步骤思考**
3. **利用上下文信息**
4. **智能补全缺失信息**
5. **常见推理模式**

### 推理示例

（提供具体的推理示例）
```

### 2. 上下文注入

在 `plugins/chat_plugin.py` 中，注入用户信息：

```python
context_info = f"[系统提示：发送此消息的用户是{sender_nickname}(QQ:{user_id})"
if group_id:
    context_info += f"，当前在群{group_id}中"
context_info += f"。你的 QQ 号是 {bot_qq}，用户的 QQ 号是 {user_id}]"

enhanced_user_input = context_info + "\n\n" + user_input
```

### 3. 工具设计

工具应该支持**自动推理**：

```python
class SendLikeTool(BaseTool):
    def _run(self, user_id: Optional[str] = None, times: int = 10):
        # 如果没有提供 user_id，自动从上下文获取
        if not user_id:
            context = get_current_context()
            # 优先使用被 @ 的用户
            mentioned_users = context.get("mentioned_users", [])
            if mentioned_users:
                user_id = mentioned_users[0]
            else:
                # 否则使用当前用户
                user_id = context.get("user_id")
        
        # 执行点赞
        ...
```

### 4. LangChain Agent

使用 LangGraph 的 ReAct Agent，它天然支持：
- 多步推理
- 工具串联
- 中间结果传递

```python
self.agent = create_react_agent(
    model=self.llm,
    tools=self.tools,
    prompt=self.system_prompt
)
```

## 效果对比

### 场景 1：发邮件

**机械式**：
```
用户：给我发个邮件
机器人：你的邮箱是什么？
用户：123456789@qq.com
机器人：好的，已发送
```
- 需要 2 轮对话
- 用户体验差

**智能式**：
```
用户：给我发个邮件
机器人：已经发到你的 QQ 邮箱啦~
```
- 只需 1 轮对话
- 用户体验好

### 场景 2：点赞

**机械式**：
```
用户：给 @张三 点赞
机器人：张三的 QQ 号是什么？
用户：123456
机器人：好的，已点赞
```

**智能式**：
```
用户：给 @张三 点赞
机器人：已经给张三点赞啦~
```

### 场景 3：定时任务

**机械式**：
```
用户：10分钟后给我发个邮件提醒吃饭
机器人：你的邮箱是什么？
用户：123456789@qq.com
机器人：好的，已设置
```

**智能式**：
```
用户：10分钟后给我发个邮件提醒吃饭
机器人：好的，10分钟后会发邮件提醒你吃饭~
```

## 扩展性

这个推理框架可以扩展到更多场景：

1. **智能搜索**
   - 用户："帮我查一下北京的天气"
   - 推理：需要天气信息 → 调用 get_weather 工具

2. **智能提醒**
   - 用户："明天早上8点提醒我开会"
   - 推理：定时任务 + 时间计算 → 调用 create_workflow

3. **智能绘图**
   - 用户："画一个知识图谱"
   - 推理：需要生成图片 → 调用 render_html 工具

4. **智能搜图**
   - 用户："给我看看猫的图片"
   - 推理：需要搜索图片 → 调用 search_image 工具

5. **复杂任务串联**
   - 用户："查一下天气，如果下雨就发邮件提醒我带伞"
   - 推理：
     1. 调用 get_weather 工具
     2. 判断是否下雨
     3. 如果下雨，推理邮箱地址
     4. 调用 send_email 工具

## 关键要点

1. **不要写死规则**：不要为每个场景写死"如果xxx就xxx"的规则
2. **依赖 AI 推理**：让 AI 自己学会推理，通过好的系统提示词
3. **提供推理示例**：在系统提示词中提供具体的推理示例
4. **工具支持自动推理**：工具设计要支持参数缺失时的自动推理
5. **充分利用上下文**：注入足够的上下文信息供 AI 推理

## 未来改进

1. **更强的推理模型**
   - 使用 DeepSeek-R1 等推理模型
   - 提供更详细的推理过程

2. **推理链可视化**
   - 显示 AI 的推理步骤
   - 帮助调试和优化

3. **推理模式学习**
   - 从历史对话中学习推理模式
   - 自动优化推理策略

4. **多模态推理**
   - 结合图片、语音等多模态信息
   - 进行更复杂的推理

## 总结

**智能推理能力是 AI 助手的核心竞争力。**

通过：
- 好的系统提示词设计
- 充分的上下文注入
- 支持自动推理的工具
- 强大的 Agent 框架（LangGraph）

我们可以让机器人从"问答机器"进化为"推理助手"，提供更智能、更自然的用户体验。

这不仅仅是技术问题，更是**设计哲学**的问题。
