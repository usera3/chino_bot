# 知识图谱问题分析与解决方案

## 问题描述

用户说"画一下 AI Agent 的知识图谱"时：
1. **第一次**：AI 只回复文字"画好啦~是AI Agent的知识图谱呢"，但**没有调用 render_html 工具**
2. **第二次**：完全没有回复（消息被接收但没有触发 chat_plugin）
3. **第三次**：同样没有回复（消息被接收但没有触发 chat_plugin）

## 问题分析

### 问题 1：第一次没有调用工具

**原因**：AI 判断"知识图谱"这个需求太复杂，无法用简单的 HTML/CSS 实现，所以选择不调用工具，只是口头回复"画好了"。

**日志证据**：
```
01-19 12:30:17 [INFO] 收到消息 [群聊]
   用户: 1446437177
   原始文本: 画一下ai agent的知识图谱

🤖 Agent 推理过程:
✅ 回复成功: 画好啦~是AI Agent的知识图谱呢(｡･ω･｡)...
```

没有看到 `🔧 调用工具: render_html` 的日志。

### 问题 2：第二次和第三次没有回复

**原因**：消息被 NoneBot 接收，但**没有触发 chat_plugin 的处理逻辑**。

**日志证据**：
```
01-19 12:30:27 [SUCCESS] nonebot | OneBot V11 2509109290 | [message.group.normal]: 
Message 139620050 from 1446437177@[群:878812866] '@智乃 画一下ai agent的知识图谱'

# 没有看到 "Event will be handled by Matcher(type='message', module=plugins.chat_plugin)"
```

**可能原因**：
1. NoneBot 的 `to_me()` 规则在短时间内收到相同内容的消息时，可能出现某种问题
2. 消息格式问题（@ 格式不同）
3. NoneBot 的某种内部机制（如消息去重）

## 解决方案

### 解决方案 1：改进 AI 提示词

在 `core/butler.py` 中，增强了关于 HTML 渲染工具的使用说明：

```python
- 用户说"帮我画一个xxx"、"生成一个xxx图"、"做一张xxx"、"画一下xxx" → **必须**调用 render_html 工具生成图片
  * 即使需求看起来复杂（如"知识图谱"、"流程图"），也要尝试用 HTML/CSS/SVG 实现
  * 如果实在无法实现，调用工具后在 HTML 中显示"抱歉，这个太复杂了"
  * **不要**只是口头回复"画好了"而不调用工具
```

**效果**：强制 AI 在遇到绘图需求时，必须调用工具，即使需求复杂。

### 解决方案 2：添加调试日志

在 `plugins/chat_plugin.py` 中，添加了一个低优先级的调试处理器：

```python
# 创建调试处理器（记录所有消息，不阻断）
debug_handler = on_message(priority=1, block=False)

@debug_handler.handle()
async def log_all_messages(event: MessageEvent):
    """记录所有消息用于调试"""
    chat_type = "群聊" if isinstance(event, GroupMessageEvent) else "私聊"
    group_id = event.group_id if isinstance(event, GroupMessageEvent) else None
    user_id = event.user_id
    text = event.get_plaintext().strip()
    
    # 检查是否 @ 了机器人
    is_tome = False
    for seg in event.message:
        if seg.type == "at":
            at_qq = seg.data.get("qq")
            if at_qq == str(event.self_id):
                is_tome = True
                break
    
    print(f"[DEBUG] 收到消息 [{chat_type}] 用户:{user_id} 群:{group_id} @我:{is_tome} 内容:{text[:50]}")
```

**效果**：可以追踪所有消息，包括那些没有触发 chat_plugin 的消息，帮助诊断 `to_me()` 规则的问题。

## 测试建议

1. **测试简单绘图**：说"生成一张五角星的图" → 应该调用 render_html 工具
2. **测试复杂绘图**：说"画一下 AI Agent 的知识图谱" → 应该调用 render_html 工具（即使结果可能不完美）
3. **测试重复消息**：连续发送相同的消息，观察是否都能触发 chat_plugin
4. **查看调试日志**：观察 `[DEBUG]` 日志，确认所有消息都被记录

## 预期效果

1. **AI 必须调用工具**：当用户说"画xxx"时，AI 必须调用 render_html 工具，不能只是口头回复
2. **调试日志可见**：所有消息都会被 debug_handler 记录，方便追踪问题
3. **重复消息处理**：如果 to_me() 规则有问题，可以通过调试日志发现

## 下一步

如果问题仍然存在：
1. 查看调试日志，确认消息是否被接收
2. 检查 `to_me()` 规则的行为
3. 考虑使用自定义规则替代 `to_me()`
4. 检查 NoneBot 的配置（如 SESSION_EXPIRE_TIMEOUT）
