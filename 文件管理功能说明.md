# 🗑️ 文件管理功能说明

## 功能概述

机器人现在可以自动管理临时文件，在发送图片/文件后自动删除，节省磁盘空间。

## 新增工具

### 1. delete_file - 删除单个文件
删除指定的文件，释放磁盘空间。

### 2. clean_temp_files - 批量清理临时文件
批量删除匹配模式的文件（如所有截图）。

## 使用场景

### 场景 1：截图后自动清理（推荐）

**工作流**：
```
1. 用户："截图 https://www.baidu.com"
2. AI 调用：web_screenshot → 生成截图
3. AI 调用：send_image → 发送给用户
4. AI 调用：delete_file → 删除临时文件
```

**效果**：
- ✅ 用户收到截图
- ✅ 临时文件自动删除
- ✅ 节省磁盘空间

### 场景 2：手动清理文件

**用户说**：
- "删除这个文件 /path/to/file.png"
- "清理一下截图文件"
- "把临时文件都删了"

### 场景 3：批量清理

**用户说**：
- "清理所有截图"
- "删除 Downloads 里的所有 screenshot 文件"

## 工具详细说明

### delete_file

**参数**：
- `file_path`: 文件完整路径（必需）

**返回示例**：
```
✅ 文件已删除
📁 文件名：screenshot_1768810956.png
📊 释放空间：495.80 KB
🗑️ 路径：/Users/xxx/Downloads/screenshot_1768810956.png
```

### clean_temp_files

**参数**：
- `pattern`: 文件匹配模式（可选，默认 `screenshot_*.png`）
- `directory`: 目录路径（可选，默认 Downloads）

**返回示例**：
```
✅ 清理完成
📁 目录：/Users/xxx/Downloads
🔍 模式：screenshot_*.png
🗑️ 删除：5 个文件
📊 释放空间：2.53 MB
```

## 测试结果

✅ **测试 1**：删除单个文件 - 成功（释放 495.80 KB）
✅ **测试 2**：批量清理 5 个文件 - 成功（释放 2.53 MB）

## 最佳实践

### 1. 截图工作流（推荐）

```python
# AI 自动执行的流程
1. web_screenshot(url="https://example.com")  # 截图
   → 返回：file_path = "/Users/xxx/Downloads/screenshot_xxx.png"

2. send_image(file_path)  # 发送给用户
   → 用户收到图片

3. delete_file(file_path)  # 删除临时文件
   → 释放空间
```

### 2. 定期清理

用户可以说：
- "清理一下临时文件"
- "删除所有截图"

AI 会调用 `clean_temp_files` 批量清理。

### 3. 手动删除

用户可以指定具体文件：
- "删除 /Users/xxx/Downloads/test.png"

## 安全机制

✅ **检查文件存在** - 删除前检查文件是否存在
✅ **确认是文件** - 不会删除目录
✅ **权限检查** - 没有权限时返回错误
✅ **删除确认** - 删除后验证文件是否真的被删除

## 工具数量

添加此功能后，机器人工具总数：**31 个** ✨

- 29 个（之前）
- +1 个（delete_file）
- +1 个（clean_temp_files）

## 空间节省效果

根据测试：
- 单张截图：约 400-800 KB
- 每天 10 张截图：约 5-8 MB
- 每月 300 张截图：约 150-240 MB

**自动清理后**：
- 磁盘占用：接近 0
- 节省空间：100%

## 注意事项

1. **不可恢复**：删除的文件无法恢复，请谨慎使用
2. **权限要求**：需要有文件的删除权限
3. **路径正确**：确保文件路径正确
4. **批量清理**：使用通配符时要小心，避免误删

## 常见问题

### Q: 删除的文件可以恢复吗？
A: 不能，文件会被永久删除

### Q: 会误删重要文件吗？
A: 不会，工具只删除指定的文件，且有多重检查

### Q: 可以删除目录吗？
A: 不可以，工具只能删除文件，不能删除目录

### Q: 批量清理会删除哪些文件？
A: 只删除匹配指定模式的文件，默认是 `screenshot_*.png`

## 下一步优化

- [ ] 添加回收站功能（可恢复）
- [ ] 添加文件大小限制
- [ ] 添加白名单保护
- [ ] 添加删除日志
- [ ] 支持定时自动清理
