# 伪造消息工具说明

## 功能概述

伪造消息工具基于 `nonebot-plugin-fakemsg` 插件，可以创建合并转发的"伪造"消息，模拟多个用户的对话场景。

## 安装

```bash
pip install nonebot-plugin-fakemsg
```

## 功能特性

### 核心功能
- ✅ 创建合并转发消息
- ✅ 模拟多个用户对话
- ✅ 支持群聊和私聊
- ✅ 自动获取用户昵称
- ✅ 支持同一用户多条消息

### 使用场景
1. **娱乐恶搞** - 制作有趣的对话截图
2. **教学演示** - 创建示例对话场景
3. **测试调试** - 模拟多人对话测试

## 使用方法

### 基本格式

```
QQ号1说内容1|QQ号2说内容2|QQ号3说内容3
```

### 示例

#### 示例 1：简单对话
```
用户: 帮我伪造一个对话，123456说你好|654321说我也好
机器人: ✅ 成功发送伪造消息到群 xxx（共 2 条消息）
```

效果：
```
[合并转发消息]
用户123456: 你好
用户654321: 我也好
```

#### 示例 2：多条消息
```
用户: 伪造对话：111111说今天天气不错 真的很好|222222说是啊 我也觉得
机器人: ✅ 成功发送伪造消息到群 xxx（共 4 条消息）
```

效果：
```
[合并转发消息]
用户111111: 今天天气不错
用户111111: 真的很好
用户222222: 是啊
用户222222: 我也觉得
```

#### 示例 3：复杂对话
```
用户: 制作一个三人对话：
      123456说大家好|
      654321说你好啊|
      789012说很高兴认识你们
```

## 工具参数

### 输入参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| messages | str | ✅ | 消息内容，格式：QQ号1说内容1\|QQ号2说内容2 |
| target_id | str | ✅ | 目标 ID（群号或用户 QQ） |
| is_group | bool | ❌ | 是否发送到群聊（默认 True） |

### 消息格式规则

1. **分隔符**
   - 使用 `|` 分隔不同用户的消息
   - 使用 `说` 连接 QQ 号和消息内容
   - 使用空格分隔同一用户的多条消息

2. **QQ 号要求**
   - 必须是 6-10 位数字
   - 必须是机器人好友或在群内
   - 白名单用户需要超级用户权限

3. **内容限制**
   - 不能包含特殊字符
   - 不能包含 CQ 码
   - 建议每条消息不超过 30 字符

## 技术实现

### 架构

```
用户请求 → Butler Agent → 伪造消息工具 → NoneBot API → QQ 合并转发
```

### 核心代码

```python
# 构建合并转发消息
forward_messages = [
    {
        "type": "node",
        "data": {
            "name": "用户昵称",
            "uin": "QQ号",
            "content": "消息内容"
        }
    }
]

# 发送到群聊
await bot.call_api(
    "send_group_forward_msg",
    group_id=群号,
    messages=forward_messages
)
```

### 文件结构

```
zhinai-bot-v3/
├── tools/
│   ├── fake_message_tool.py      # 工具实现
│   └── basic_tools.py             # 工具注册
└── 伪造消息工具说明.md            # 本文档
```

## 安全限制

### 白名单机制

配置文件 `.env` 中可设置白名单：

```env
# 无法被伪造的 QQ 号（用逗号分隔）
FAKEMSG_WHITELIST=123456,654321
```

- 白名单用户无法被普通用户伪造
- 超级用户（SUPERUSERS）不受限制

### 使用限制

⚠️ **重要提醒**：

1. **仅用于娱乐和教学目的**
2. **不得用于欺骗、诈骗或其他违法行为**
3. **不得冒充他人进行恶意行为**
4. **使用者需承担相应法律责任**

## 配置选项

在 `.env` 文件中添加：

```env
# 用户分隔符（默认：|）
USER_SPLIT=|

# 消息分隔符（默认：空格）
MESSAGE_SPLIT= 

# 白名单（无法被伪造的 QQ 号）
FAKEMSG_WHITELIST=
```

## 错误处理

### 常见错误

1. **QQ 号格式错误**
   ```
   ❌ QQ 号格式错误: xxx（必须是 6-10 位数字）
   ```
   解决：确保 QQ 号是 6-10 位纯数字

2. **消息格式错误**
   ```
   ❌ 消息格式错误: xxx（应为：QQ号说内容）
   ```
   解决：检查是否使用了"说"字连接

3. **无法获取 Bot 实例**
   ```
   ❌ 无法获取 Bot 实例，请确保机器人正在运行
   ```
   解决：确保机器人已启动并连接

4. **发送失败**
   ```
   ❌ 发送失败: xxx
   ```
   解决：检查目标群号/QQ 号是否正确，机器人是否有权限

## 使用示例

### 场景 1：制作搞笑对话

```
用户: 帮我做个搞笑对话：
      123456说今天吃什么|
      654321说吃你|
      123456说？？？
      
机器人: ✅ 成功发送伪造消息到群 xxx（共 3 条消息）
```

### 场景 2：教学演示

```
用户: 创建一个客服对话示例：
      客服001说您好，有什么可以帮您|
      用户002说我想查询订单|
      客服001说请提供订单号
      
机器人: ✅ 成功发送伪造消息到群 xxx（共 3 条消息）
```

### 场景 3：测试场景

```
用户: 模拟三人讨论：
      111111说这个功能怎么实现|
      222222说可以用 API|
      333333说我来写代码
      
机器人: ✅ 成功发送伪造消息到群 xxx（共 3 条消息）
```

## 工具调用日志

```
🤖 Agent 推理过程:
   🔧 调用工具: send_fake_message
   📥 输入: {
       'messages': '123456说你好|654321说我也好',
       'target_id': '群号',
       'is_group': True
   }
   📤 工具输出: ✅ 成功发送伪造消息到群 xxx（共 2 条消息）
```

## 注意事项

1. **权限要求**
   - 机器人必须在目标群内
   - 机器人需要发送消息权限
   - 伪造的 QQ 号必须是机器人好友或在群内

2. **性能考虑**
   - 每次调用会查询用户信息
   - 建议不要一次伪造过多消息
   - 合并转发有消息数量限制

3. **兼容性**
   - 仅支持 OneBot V11 协议
   - 需要 NoneBot2 环境
   - 部分客户端可能显示异常

## 后续优化

### 短期
- ✅ 基础功能实现
- ✅ 错误处理完善
- ⏳ 添加消息预览
- ⏳ 支持图片消息

### 长期
- 支持更多消息类型（图片、表情等）
- 添加消息模板功能
- 支持批量伪造
- 添加消息历史记录

## 总结

伪造消息工具已成功集成到 v3 机器人中，作为 LangChain Tool 使用。用户可以通过自然语言指令创建合并转发的伪造消息，适用于娱乐、教学和测试场景。

⚠️ **请合理使用，遵守法律法规！**

---

**更新时间**: 2025-01-19  
**版本**: v1.0  
**状态**: ✅ 已完成并测试通过
