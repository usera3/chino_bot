# 智能编程助手 - 快速参考 v2.0

## 📋 工具总览

### 阶段 1：基础代码操作（5 个工具）✅
1. `read_project_file` - 读取项目文件
2. `write_project_file` - 写入项目文件（需管理员权限）
3. `list_project_files` - 列出项目文件
4. `search_in_files` - 搜索文件内容
5. `get_directory_tree` - 获取目录树

### 阶段 2：代码理解能力（6 个工具）✅
6. `analyze_code_structure` - 分析代码结构
7. `get_function_definition` - 获取函数/类定义
8. `analyze_dependencies` - 分析依赖关系
9. `run_tests` - 运行测试
10. `get_error_context` - 获取错误上下文
11. `analyze_test_coverage` - 分析测试覆盖率

### 阶段 3：智能编程能力（1 个工具）🔄
12. `plan_coding_task` - 规划编程任务 ✅

---

## 🎯 常用场景

### 场景 1：查看代码
```
用户: 读取 bot.py
→ 调用 read_project_file("bot.py")
```

### 场景 2：搜索代码
```
用户: 在所有 Python 文件中搜索 Butler
→ 调用 search_in_files("Butler", "*.py", ".")
```

### 场景 3：分析代码
```
用户: 分析 butler.py 的结构
→ 调用 analyze_code_structure("core/butler.py")
```

### 场景 4：查找定义
```
用户: Butler 类在哪里定义的
→ 调用 get_function_definition("core/butler.py", "Butler", "class")
```

### 场景 5：运行测试
```
用户: 运行 test_butler.py 的测试
→ 调用 run_tests("tests/test_butler.py")
```

### 场景 6：规划任务（新功能）
```
用户: 帮我添加一个翻译工具
→ 调用 plan_coding_task("添加一个翻译工具")
→ 返回：
  - 任务类型: 添加功能
  - 步骤: 7 个
  - 预计时间: 54分钟
  - 依赖关系
```

---

## 📊 任务规划器详解

### 支持的任务类型
1. **添加功能** - 创建新工具、新模块
2. **修复 Bug** - 定位和修复错误
3. **重构代码** - 改进代码结构
4. **优化性能** - 提升运行效率
5. **添加测试** - 编写测试用例
6. **更新文档** - 更新说明文档

### 规划输出
```
📋 任务规划: [任务描述]
🎯 任务类型: [类型]
📝 提取信息: [关键信息]
⏱️ 预计时间: [总时间]
📌 执行步骤: [详细步骤]
🔗 依赖关系: [依赖]
💡 建议: [实用建议]
```

### 使用示例

#### 示例 1：添加功能
```
输入: 帮我添加一个翻译工具
输出:
  - 任务类型: 添加功能
  - 步骤数: 7
  - 预计时间: 54分钟
  - 步骤:
    1. 分析需求 (5分钟)
    2. 创建文件 (2分钟)
    3. 实现功能 (20分钟)
    4. 注册工具 (5分钟)
    5. 编写测试 (15分钟)
    6. 运行测试 (2分钟)
    7. 更新文档 (5分钟)
```

#### 示例 2：修复 Bug
```
输入: 修复 butler.py 第 100 行的错误
输出:
  - 任务类型: 修复Bug
  - 提取信息:
    - 文件: butler.py
    - 行号: 100
  - 步骤数: 5
  - 预计时间: 25分钟
  - 步骤:
    1. 定位错误 (3分钟)
    2. 分析代码 (5分钟)
    3. 识别原因 (5分钟)
    4. 修复代码 (10分钟)
    5. 运行测试 (2分钟)
```

#### 示例 3：重构代码
```
输入: 重构 code_analyzer.py
输出:
  - 任务类型: 重构代码
  - 提取信息:
    - 文件: code_analyzer.py
  - 步骤数: 5
  - 预计时间: 43分钟
  - 步骤:
    1. 分析当前结构 (5分钟)
    2. 识别问题 (5分钟)
    3. 规划方案 (10分钟)
    4. 重构代码 (20分钟)
    5. 运行测试 (3分钟)
```

---

## 🔧 工具详细说明

### 1. read_project_file
**功能**：读取项目文件内容

**参数**：
- `file_path` (str)：文件路径

**返回**：文件内容和基本信息

**示例**：
```python
read_project_file("bot.py")
```

---

### 2. write_project_file
**功能**：写入项目文件（需管理员权限）

**参数**：
- `file_path` (str)：文件路径
- `content` (str)：文件内容

**返回**：操作结果

**示例**：
```python
write_project_file("tools/new_tool.py", "# 新工具代码")
```

**注意**：
- 只有管理员（QQ: 123456789）可以执行
- 所有操作会被审计记录

---

### 3. list_project_files
**功能**：列出项目文件

**参数**：
- `directory` (str)：目录路径
- `pattern` (str)：文件模式（可选）
- `recursive` (bool)：是否递归（可选）

**返回**：文件列表

**示例**：
```python
list_project_files("tools", "*.py", True)
```

---

### 4. search_in_files
**功能**：搜索文件内容

**参数**：
- `pattern` (str)：搜索模式
- `file_pattern` (str)：文件模式（可选）
- `directory` (str)：目录路径（可选）

**返回**：搜索结果（文件名、行号、内容）

**示例**：
```python
search_in_files("Butler", "*.py", ".")
```

---

### 5. get_directory_tree
**功能**：获取目录树

**参数**：
- `directory` (str)：目录路径
- `max_depth` (int)：最大深度（可选）

**返回**：树形结构

**示例**：
```python
get_directory_tree(".", 2)
```

---

### 6. analyze_code_structure
**功能**：分析代码结构

**参数**：
- `file_path` (str)：文件路径

**返回**：
- 类列表
- 函数列表
- 导入列表
- 代码行数
- 圈复杂度

**示例**：
```python
analyze_code_structure("core/butler.py")
```

---

### 7. get_function_definition
**功能**：获取函数/类定义

**参数**：
- `file_path` (str)：文件路径
- `name` (str)：函数/类名
- `definition_type` (str)：类型（"function" 或 "class"）

**返回**：
- 定义类型
- 文件位置
- 行号
- 完整代码
- 文档字符串

**示例**：
```python
get_function_definition("core/butler.py", "Butler", "class")
```

---

### 8. analyze_dependencies
**功能**：分析依赖关系

**参数**：
- `file_path` (str)：文件路径

**返回**：
- 标准库
- 第三方库
- 项目内模块

**示例**：
```python
analyze_dependencies("bot.py")
```

---

### 9. run_tests
**功能**：运行测试

**参数**：
- `test_path` (str)：测试路径（可选）
- `test_name` (str)：测试名称（可选）
- `verbose` (bool)：详细输出（可选）

**返回**：
- 测试结果
- 统计信息
- 错误信息

**示例**：
```python
run_tests("tests/test_butler.py")
run_tests("tests/test_butler.py", "test_process")
```

---

### 10. get_error_context
**功能**：获取错误上下文

**参数**：
- `file_path` (str)：文件路径
- `line_number` (int)：行号
- `context_lines` (int)：上下文行数（可选，默认 5）

**返回**：错误行及其上下文

**示例**：
```python
get_error_context("core/butler.py", 50)
```

---

### 11. analyze_test_coverage
**功能**：分析测试覆盖率

**参数**：无

**返回**：
- 测试文件统计
- 代码文件统计
- 覆盖率百分比
- 未测试文件列表

**示例**：
```python
analyze_test_coverage()
```

---

### 12. plan_coding_task（新功能）
**功能**：规划编程任务

**参数**：
- `description` (str)：任务描述

**返回**：
- 任务类型
- 提取信息
- 执行步骤
- 时间估算
- 依赖关系
- 实用建议

**示例**：
```python
plan_coding_task("添加一个翻译工具")
plan_coding_task("修复 butler.py 第 100 行的错误")
plan_coding_task("重构 code_analyzer.py")
```

**支持的任务类型**：
- 添加功能
- 修复 Bug
- 重构代码
- 优化性能
- 添加测试
- 更新文档

---

## 🎯 最佳实践

### 1. 开始复杂任务前先规划
```
用户: 帮我添加一个翻译工具
机器人: [先调用 plan_coding_task 规划]
       [然后按步骤执行]
```

### 2. 按步骤顺序执行
```
步骤 1 → 步骤 2 → 步骤 3 → ...
每步完成后进行测试
```

### 3. 遇到错误及时查看上下文
```
测试失败 → get_error_context 查看错误
         → 分析原因
         → 修复代码
         → 重新测试
```

### 4. 定期运行测试
```
修改代码后 → run_tests 验证
           → 确保功能正常
```

### 5. 保持代码质量
```
完成功能后 → analyze_code_structure 检查
           → 优化代码结构
           → 添加测试
```

---

## 📊 工具统计

### 总工具数：46 个
- 基础工具：6 个
- LangChain 工具：10 个
- QQ 互动工具：8 个
- 文件工具：5 个
- 系统工具：3 个
- 代码工具：8 个
- 分析工具：3 个
- 测试工具：3 个
- 规划工具：1 个（新增）

### 阶段进度
- ✅ 阶段 1：基础代码操作（100%）
- ✅ 阶段 2：代码理解能力（100%）
- 🔄 阶段 3：智能编程能力（25%）
  - ✅ 任务规划器（100%）
  - ⏳ 代码修改工具（0%）
  - ⏳ 代码审查工具（0%）
  - ⏳ 自动修复工具（0%）

---

## 🚀 即将推出

### 代码修改工具（Day 17-20）
- `apply_code_changes` - 应用代码更改
- `preview_changes` - 预览更改

### 代码审查工具（Day 21-24）
- `review_code` - 审查代码质量

### 自动修复工具（Day 25-28）
- `auto_fix_error` - 自动修复错误
- `generate_tests` - 生成测试

---

**更新时间**：2026-01-19  
**版本**：v2.0  
**工具总数**：46 个

祝使用愉快！🎉
