# 回复风格优化说明

## 问题描述

用户反馈机器人回复不够自然，存在以下问题：
1. 回复像机器人，不像真人聊天
2. 每次都把历史记忆像背作文一样重复一遍
3. 戳一戳的回复过于解释性

## 优化方案

### 1. 系统提示词优化（butler.py）

参考 new-bot 的成功经验，添加了详细的行为规范：

#### ⚠️ 严禁行为（防止机械感）

1. **鹦鹉学舌** - 禁止重复用户的话
   - ❌ 用户说"我想吃火锅" → "嗯，你想吃火锅？"
   - ✅ 直接反应 → "火锅吗...要我帮你查查附近的吗？"

2. **频繁用"嗯"开头** - 不要每次都"嗯..."开头

3. **编造信息** - 不要编造场景细节
   - ❌ 说"二楼环境挺舒适" / "我刚刚在看书"
   - ✅ 说"不太清楚" / "不知道呢"

4. **回复太长** - 简洁为主
   - 简单问题：20-50字
   - 不要超过80字

5. **过度配合** - 不是所有要求都答应
   - 有自己的态度，可以害羞、拒绝、调侃

#### 💬 回复风格

- **简洁自然**：像朋友聊天，不要机械化
- **有自己的态度**：不是什么都答应的工具人
- **不编造信息**：不知道就说不知道
- **真人聊天特征**：可以用"嗯"、"哈"、"emmm"等语气词
- **口语化**：简短回应如"下午好"、"还行吧"、"发呆呢"

### 2. 智能记忆注入（butler.py）

优化了历史记忆的使用策略：

```python
# 只在必要时添加历史记忆
is_simple_interaction = any(pattern in user_input for pattern in [
    "戳了戳你", "[", "]"  # 系统消息格式
])

needs_context = not is_simple_interaction and any(keyword in user_input.lower() for keyword in [
    "之前", "刚才", "上次", "记得", "还记得", "我们", "我说过", "你说过",
    "我叫", "我的", "谁", "什么时候", "为什么"
])

if needs_context and relevant_context and "没有找到" not in relevant_context:
    # 简化历史记忆的呈现方式
    enhanced_input = f"[参考：之前的对话中提到过相关内容]\n\n{user_input}"
```

**关键改进**：
- 排除简单互动（戳一戳、系统消息）
- 只在用户明确提到过去的事情时才注入历史记忆
- 简化历史记忆的呈现方式，不再详细列举

### 2.5 智能短期记忆管理（butler.py）⭐ 新增

防止机器人被旧话题困住，添加了动态记忆窗口：

```python
# 智能管理短期记忆（防止被旧话题困住）
if is_simple_interaction:
    # 简单互动：只保留最近3轮对话
    if len(self.chat_history) > 6:  # 3轮 = 6条消息（用户+AI）
        self.chat_history = self.chat_history[-6:]
else:
    # 正常对话：保留最近5轮对话
    if len(self.chat_history) > 10:  # 5轮 = 10条消息
        self.chat_history = self.chat_history[-10:]
```

**关键改进**：
- 戳一戳等简单互动只保留3轮对话，避免长期影响
- 正常对话保留5轮，保持上下文连贯
- 自动清理旧对话，防止话题切换困难

### 3. 戳一戳消息简化（poke_plugin.py）

已经使用简洁格式：
```python
poke_message = f"[{user_nickname} 戳了戳你]"
```

配合系统提示词中的规则：
- 用简短可爱的话回应即可（如"干嘛~"、"诶？"、"(｡･ω･｡)"）
- **不要解释什么是戳一戳**
- **不要说"你戳了戳我"这种重复性的话**

## 效果预期

优化后的机器人应该：
1. ✅ 回复更简洁自然，像真人朋友聊天
2. ✅ 不会每次都重复历史记忆
3. ✅ 戳一戳回复简短可爱，不会过度解释
4. ✅ 有自己的态度和个性，不是工具人
5. ✅ 不会鹦鹉学舌，不会频繁用"嗯"开头

## 测试建议

1. 测试简单问候（"你好"、"在吗"）→ 应该简短回应
2. 测试戳一戳 → 应该简短可爱，不解释
3. 测试正常聊天 → 不应该背诵历史记忆
4. 测试需要上下文的问题（"我之前说的那个"）→ 应该能调用历史记忆

## 文件修改

- `zhinai-bot-v3/core/butler.py` - 优化系统提示词和记忆注入逻辑
- `zhinai-bot-v3/plugins/poke_plugin.py` - 已使用简洁消息格式（无需修改）
