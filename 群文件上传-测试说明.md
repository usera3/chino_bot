# 群文件上传功能 - 测试说明

## 当前状态

✅ **已修复路径问题**
- 修改了 `tools/document_tools.py`
- Word 和 Excel 文档现在保存到 `~/Downloads/` 目录
- NapCat 可以访问 Downloads 目录（通过沙盒符号链接）

✅ **机器人已重启**
- 当前运行的是 **zhinai-bot-v3** (PID: 47776)
- 新代码已生效

## 测试步骤

在 QQ 群中发送以下消息测试：

```
@智乃 随便写个word文档上传到群文件用于测试该功能
```

## 预期结果

1. ✅ Word 文档创建成功
   - 文件保存在：`~/Downloads/test_document.docx`
   - 控制台输出：`⚠️ 路径已修改为: ~/Downloads/test_document.docx`

2. ✅ 群文件上传成功
   - NapCat 可以访问 Downloads 目录
   - 文件成功上传到群文件

3. ✅ 或者聊天发送文件成功
   - 如果上传失败，会尝试在聊天中发送文件

## 技术细节

### 修复前的路径
```
/path/to/chino_bot/test_document.docx
```
❌ NapCat 无法访问（沙盒限制）

### 修复后的路径
```
~/Downloads/test_document.docx
```
✅ NapCat 可以访问（沙盒有符号链接）

### 沙盒结构
```
/Users/mozi100/Library/Containers/com.tencent.qq/Data/
├── Downloads -> ../../../../Downloads  ✅ 可访问
├── Desktop -> ../../../../Desktop      ✅ 可访问
└── Documents/                          ✅ 可访问
```

## 如果还是失败

如果测试后还是失败，请查看日志：

```bash
# 查看 Bot 日志
tail -50 logs/bot.log

# 查看 QQ 日志
tail -50 logs/qq.log
```

查找关键信息：
- ✅ 是否有 `⚠️ 路径已修改为: ~/Downloads/...`
- ❌ 是否还有 `EPERM: operation not permitted, copyfile`
- 📁 文件路径是否正确

## 备用方案

如果 Downloads 目录也不行，可以尝试：
1. Desktop 目录：`~/Desktop/`
2. Documents 目录：`~/Documents/`

修改 `tools/document_tools.py` 中的：
```python
downloads_dir = os.path.expanduser("~/Downloads")
# 改为
downloads_dir = os.path.expanduser("~/Desktop")
```

## 注意事项

- 文件会保存在 Downloads 目录，用户可以看到
- 建议在文件名中添加前缀（如 `bot_`）以便识别
- 上传成功后可以选择删除文件
