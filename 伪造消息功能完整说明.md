# 伪造消息功能完整说明

## 功能概述

伪造消息工具可以创建合并转发的"伪造"消息，模拟多个用户的对话场景。基于 OneBot V11 的 `send_forward_msg` API 实现。

## 当前状态

### ✅ 已实现

1. **消息解析** - 正确解析多个用户的对话
2. **昵称获取** - 自动获取 QQ 用户昵称
3. **占位符替换** - 支持 `{{USER_QQ}}` 和 `{{BOT_QQ}}` 占位符
4. **上下文识别** - 自动识别当前是群聊还是私聊
5. **API 调用** - 正确调用 OneBot API

### ⚠️ 已知问题

1. **私聊显示异常**
   - 现象：私聊中的合并转发消息可能只显示一个发送者
   - 原因：NapCat 对 ARM64 架构支持不完整
   - 日志：`PacketBackend 不支持当前QQ版本架构：6.9.81-40366-arm64`

2. **群聊未测试**
   - 群聊的合并转发通常支持度更好
   - 需要在群聊中测试验证

## 技术分析

### 代码层面（✅ 正常）

从调试日志可以看到：

```
[DEBUG] 当前上下文: {'user_id': '1446437177', 'group_id': None}
[DEBUG] target_user_id: 1446437177
[DEBUG] bot_qq: 2509109290
[DEBUG] 最终发送目标: 用户 1446437177 (is_group=False)
[DEBUG] target_id == bot_qq? False

[DEBUG] 消息列表前3条: [
    ('空白', '1446437177', '今天天气真好呢'), 
    ('智乃', '2509109290', '是啊，和你在一起心情都变好了'), 
    ('空白', '1446437177', '那要不要一起喝奶茶')
]

[DEBUG] 准备发送到 用户 1446437177
[DEBUG] 合并转发消息数量: 4
✅ 成功发送伪造消息到用户 1446437177（共 4 条消息）
```

**结论**：
- ✅ 目标用户正确（1446437177，不是机器人）
- ✅ 消息包含两个不同的 QQ 号和昵称
- ✅ API 调用成功
- ✅ 消息格式正确

### OneBot 实现层面（❌ 异常）

NapCat 报错：
```
[error] [Core] [Packet] PacketBackend 不支持当前QQ版本架构：6.9.81-40366-arm64
```

**问题**：
- NapCat 不完全支持 ARM64 架构的 QQ
- 可能导致某些 API 功能异常
- 特别是合并转发这种复杂功能

## 解决方案

### 方案 1：在群聊中使用（推荐）

群聊的合并转发支持度通常更好。

**测试步骤**：
1. 将机器人拉入一个测试群
2. 在群里发送：`帮我伪造我和你的对话，4句`
3. 查看合并转发消息是否正确显示两个人

**如果群聊正常**：
- 说明是私聊的 API 限制
- 可以限制功能只在群聊中使用

**如果群聊也不正常**：
- 说明是 NapCat 的问题
- 需要更换 OneBot 实现

### 方案 2：更换 OneBot 实现

#### go-cqhttp（推荐）

**优点**：
- 最成熟的 OneBot 实现
- 合并转发支持完善
- 文档齐全

**缺点**：
- 基于旧版 QQ 协议
- 可能随时失效

**安装**：
```bash
# 下载 go-cqhttp
wget https://github.com/Mrs4s/go-cqhttp/releases/latest/download/go-cqhttp_darwin_arm64.tar.gz
tar -xzf go-cqhttp_darwin_arm64.tar.gz

# 运行
./go-cqhttp
```

#### Lagrange

**优点**：
- 基于新版 NTQQ 协议
- 更稳定
- 支持 ARM64

**缺点**：
- 相对较新
- 功能可能不如 go-cqhttp 完善

#### LLOneBot

**优点**：
- 基于 LiteLoaderQQNT
- 直接使用官方 QQ 客户端
- 稳定性好

**缺点**：
- 需要安装 QQNT 和 LiteLoader
- 配置相对复杂

### 方案 3：降级实现

如果合并转发确实不支持，可以使用替代方案：

#### 3.1 发送多条普通消息

```python
# 不使用合并转发，直接发送多条消息
for user_name, user_qq, content in fake_msg_list:
    await bot.send(f"{user_name}: {content}")
```

**优点**：兼容性好  
**缺点**：不是合并转发，刷屏

#### 3.2 生成对话图片

```python
# 使用 PIL 生成对话截图
from PIL import Image, ImageDraw, ImageFont

# 绘制对话界面
# 发送图片
```

**优点**：视觉效果好  
**缺点**：实现复杂

#### 3.3 文本格式展示

```python
# 生成文本格式的对话
dialogue = "\n".join([
    f"{name}: {content}"
    for name, qq, content in fake_msg_list
])
await bot.send(f"【对话记录】\n{dialogue}")
```

**优点**：简单  
**缺点**：不够美观

### 方案 4：更新 NapCat

检查是否有支持 ARM64 的新版本：

```bash
# 查看 NapCat 版本
cat napcat/package.json | grep version

# 更新 NapCat
# 访问 https://github.com/NapNeko/NapCatQQ
# 下载最新版本
```

## 测试计划

### 测试 1：群聊测试

```
步骤：
1. 将机器人拉入测试群
2. 发送：帮我伪造我和你的对话，4句
3. 查看合并转发消息

预期结果：
- 显示两个不同的发送者（用户和机器人）
- 对话内容正确
- 昵称正确

实际结果：
[ ] 通过
[ ] 失败：_______
```

### 测试 2：私聊测试

```
步骤：
1. 私聊机器人
2. 发送：帮我伪造我和你的对话，4句
3. 查看合并转发消息

预期结果：
- 显示两个不同的发送者
- 对话内容正确

实际结果：
[ ] 通过
[ ] 失败：只显示一个发送者
```

### 测试 3：多人对话测试

```
步骤：
1. 在群里发送：伪造三个人讨论技术的对话
2. 查看合并转发消息

预期结果：
- 显示三个不同的发送者
- 对话内容正确

实际结果：
[ ] 通过
[ ] 失败：_______
```

## 代码实现

当前实现已经是正确的，核心代码：

```python
# 构建合并转发消息
forward_messages = [
    {
        "type": "node",
        "data": {
            "name": user_name,    # 昵称
            "uin": user_qq,       # QQ号
            "content": content    # 消息内容
        }
    }
    for user_name, user_qq, content in fake_msg_list
]

# 发送
if is_group:
    await bot.call_api(
        "send_group_forward_msg",
        group_id=int(target_id),
        messages=forward_messages
    )
else:
    await bot.call_api(
        "send_private_forward_msg",
        user_id=int(target_id),
        messages=forward_messages
    )
```

这个实现符合 OneBot V11 标准，问题出在 NapCat 的实现上。

## 下一步行动

1. **立即测试**：在群聊中测试功能
2. **如果群聊正常**：限制功能只在群聊中使用
3. **如果群聊也不正常**：考虑更换 OneBot 实现
4. **长期方案**：等待 NapCat 更新 ARM64 支持

## 参考资料

- [OneBot V11 标准](https://github.com/botuniverse/onebot-11)
- [go-cqhttp 文档](https://docs.go-cqhttp.org/)
- [NapCat GitHub](https://github.com/NapNeko/NapCatQQ)
- [合并转发消息讨论](https://github.com/Mrs4s/go-cqhttp/discussions/926)

---

**更新时间**: 2025-01-19 02:10  
**状态**: 🔍 等待群聊测试结果  
**建议**: 优先在群聊中测试，如果正常则限制为群聊专用功能
