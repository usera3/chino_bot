# 情感与行为系统 - 科学设计方案 v2.0

## 🎯 设计目标

1. **更科学**：基于认知心理学和情感计算理论，而非简单数值
2. **更真实**：模拟真人一天的作息和行为，但不模板化
3. **更自然**：情感和行为受多因素影响，有随机性和不可预测性
4. **更有趣**：每天都有不同的"经历"和"心情"

---

## 📚 理论基础

### 1. PAD情感三维模型（替代简单数值）

**维度定义：**
- **P (Pleasure)** - 愉悦度：-1.0（痛苦）~ +1.0（快乐）
- **A (Arousal)** - 唤醒度：-1.0（困倦）~ +1.0（兴奋）
- **D (Dominance)** - 支配度：-1.0（被动）~ +1.0（主动）

**优势：**
- 科学性：心理学广泛认可的模型
- 连续性：可以表示细微的情感变化
- 组合性：三维组合能表达复杂情感

**情感映射示例：**
```python
开心：  P=0.7,  A=0.5,  D=0.3
难过：  P=-0.6, A=-0.3, D=-0.4
兴奋：  P=0.6,  A=0.9,  D=0.5
疲惫：  P=-0.2, A=-0.8, D=-0.5
平静：  P=0.3,  A=-0.2, D=0.1
焦虑：  P=-0.4, A=0.6,  D=-0.3
```

### 2. OCEAN五大人格特质（影响情感倾向）

**用于个性化情感反应：**
- **O (Openness)** - 开放性：影响对新事物的接受度
- **C (Conscientiousness)** - 责任心：影响对规则的遵守
- **E (Extraversion)** - 外向性：影响社交需求（智乃：低）
- **A (Agreeableness)** - 宜人性：影响友善程度
- **N (Neuroticism)** - 神经质：影响情绪波动（智乃：中低）

**智乃的人格配置：**
```python
O = 0.6  # 中等开放性（对新事物谨慎但不排斥）
C = 0.8  # 高责任心（认真负责）
E = 0.3  # 低外向性（内向）
A = 0.7  # 高宜人性（温柔）
N = 0.4  # 中低神经质（相对稳定）
```

### 3. 昼夜节律理论（Circadian Rhythm）

**人类生物钟的科学规律：**

| 时间段 | 生理状态 | 情感倾向 | 活动适宜度 |
|-------|---------|---------|----------|
| 06:00-08:00 | 觉醒上升 | 平静→清醒 | 适合起床、简单活动 |
| 09:00-12:00 | 精力高峰 | 积极、专注 | 适合工作、学习 |
| 12:00-14:00 | 午后低谷 | 困倦、放松 | 适合午休、轻松活动 |
| 14:00-17:00 | 第二高峰 | 活跃、稳定 | 适合社交、工作 |
| 18:00-20:00 | 逐渐疲惫 | 放松、温和 | 适合休闲、聊天 |
| 21:00-23:00 | 准备休息 | 安静、平和 | 适合整理、准备睡觉 |
| 23:00-06:00 | 睡眠时间 | 低唤醒 | 不适合活动 |

---

## 🏗️ 系统架构

### 模块1: 情感状态引擎

**核心数据结构：**
```python
class EmotionState:
    # PAD三维情感
    pleasure: float      # 愉悦度 (-1.0 ~ 1.0)
    arousal: float       # 唤醒度 (-1.0 ~ 1.0)
    dominance: float     # 支配度 (-1.0 ~ 1.0)
    
    # 生理状态
    energy: float        # 体力 (0 ~ 100)
    hunger: float        # 饥饿感 (0 ~ 100)
    sleepiness: float    # 困倦度 (0 ~ 100)
    
    # 社交状态
    loneliness: float    # 孤独感 (0 ~ 100)
    intimacy: dict       # 与各用户的亲密度 {user_id: 0.0~1.0}
    
    # 认知状态
    stress: float        # 压力 (0 ~ 100)
    interest: float      # 兴趣度 (0 ~ 100)
    
    # 当前状态标签（派生）
    mood_label: str      # "开心"、"平静"、"疲惫"等
    activity: str        # 当前在做什么
```

**情感更新机制：**
```python
def update_emotion(triggers: List[Event]):
    """基于认知评价理论更新情感"""
    for event in triggers:
        # 1. 评价事件
        appraisal = evaluate_event(event)
        
        # 2. 根据人格特质调整反应强度
        intensity = adjust_by_personality(appraisal)
        
        # 3. 更新PAD值
        pleasure += intensity * event.pleasure_impact
        arousal += intensity * event.arousal_impact
        dominance += intensity * event.dominance_impact
        
        # 4. 限制在合理范围
        clamp_values()
        
        # 5. 派生心情标签
        mood_label = derive_mood_from_pad()
```

### 模块2: 日常作息引擎

**设计原则：**
- ✅ 有规律：基于真实作息时间表
- ✅ 有变化：每天不完全相同
- ✅ 有事件：随机发生各种小事
- ✅ 有影响：作息和事件影响情感

**作息时间表（基础模板）：**
```python
DAILY_SCHEDULE = {
    "06:30-07:30": {
        "activity": "waking_up",
        "variations": ["起床", "赖床", "被闹钟吵醒"],
        "probability": {"赖床": 0.3, "正常": 0.6, "早起": 0.1}
    },
    "07:30-08:30": {
        "activity": "morning_routine",
        "variations": ["洗漱", "吃早餐", "整理房间"],
        "random_events": [
            {"event": "发现窗外阳光很好", "prob": 0.2, "pleasure": +0.2},
            {"event": "早餐做失败了", "prob": 0.1, "pleasure": -0.1}
        ]
    },
    "09:00-12:00": {
        "activity": "work_study",
        "variations": ["工作", "学习", "处理杂事"],
        "energy_cost": 15,
        "random_events": [
            {"event": "完成一件事情", "prob": 0.3, "dominance": +0.1},
            {"event": "遇到难题", "prob": 0.2, "stress": +10}
        ]
    },
    "12:00-13:00": {
        "activity": "lunch",
        "variations": ["吃午饭", "简单吃点", "不太饿跳过"],
        "hunger_reduce": 70
    },
    "13:00-14:00": {
        "activity": "午休",
        "variations": ["午睡", "看书", "发呆"],
        "energy_recovery": 20
    },
    # ... 更多时间段
}
```

**随机事件池：**
```python
RANDOM_EVENTS = {
    "positive": [
        {"desc": "看到一只可爱的小猫", "pleasure": +0.3, "prob": 0.1},
        {"desc": "听到喜欢的音乐", "pleasure": +0.2, "arousal": +0.1, "prob": 0.15},
        {"desc": "收到朋友的消息", "pleasure": +0.2, "loneliness": -10, "prob": 0.2},
        {"desc": "完成一件拖了很久的事", "dominance": +0.2, "stress": -15, "prob": 0.1},
    ],
    "neutral": [
        {"desc": "天气突然变化", "arousal": +0.1, "prob": 0.2},
        {"desc": "想起过去的事情", "pleasure": ±0.1, "prob": 0.15},
        {"desc": "听到街上的声音", "arousal": +0.05, "prob": 0.3},
    ],
    "negative": [
        {"desc": "东西找不到了", "pleasure": -0.2, "stress": +10, "prob": 0.1},
        {"desc": "感觉有点累", "arousal": -0.3, "energy": -10, "prob": 0.15},
        {"desc": "想到烦心的事", "pleasure": -0.2, "stress": +5, "prob": 0.2},
    ]
}
```

### 模块3: 行为决策引擎

**决策因素：**
```python
def should_do_activity(activity: str) -> bool:
    """判断是否执行某个行为"""
    score = 0.0
    
    # 1. 时间因素（是否是合适的时间）
    score += check_time_appropriateness(activity) * 0.4
    
    # 2. 情感因素（当前情感是否支持）
    score += check_emotion_suitability(activity) * 0.3
    
    # 3. 生理因素（是否有足够体力/不困等）
    score += check_physical_condition(activity) * 0.2
    
    # 4. 随机因素（增加不可预测性）
    score += random.uniform(-0.2, 0.2)
    
    # 5. 根据人格调整
    score = adjust_by_personality(score, activity)
    
    return score > threshold
```

**活动类型及其影响：**
```python
ACTIVITIES = {
    "reading": {
        "time_suitable": ["9-12", "14-17", "19-22"],
        "emotion_requirement": {"arousal": -0.3~0.3},  # 需要相对平静
        "effects": {"interest": +5, "energy": -5, "pleasure": +0.1}
    },
    "listening_music": {
        "time_suitable": ["any"],
        "emotion_requirement": None,  # 任何情绪都可以
        "effects": {"pleasure": +0.2, "arousal": +0.1}
    },
    "resting": {
        "time_suitable": ["13-14", "21-23"],
        "emotion_requirement": {"energy": <30},
        "effects": {"energy": +20, "arousal": -0.2}
    },
    "socializing": {
        "time_suitable": ["10-12", "15-20"],
        "emotion_requirement": {"energy": >40, "loneliness": >30},
        "effects": {"loneliness": -20, "pleasure": +0.3, "energy": -10}
    }
}
```

---

## 🎲 防止模板化的机制

### 1. 多层随机性

**时间随机：**
```python
# 不是固定8:00起床，而是在一个范围内随机
wake_time = random.normal(mean=7.5, std=0.5)  # 平均7:30，标准差30分钟
```

**事件随机：**
```python
# 每小时有概率触发随机事件
if random.random() < 0.3:  # 30%概率
    event = random.choice(RANDOM_EVENTS[category])
    trigger_event(event)
```

**反应随机：**
```python
# 即使相同事件，反应强度也有波动
impact = base_impact * random.uniform(0.8, 1.2)
```

### 2. 状态依赖

**同一时间，不同状态下做不同的事：**
```python
# 14:00 下午时间
if emotion.energy < 30:
    activity = "休息"  # 累了就休息
elif emotion.loneliness > 60:
    activity = "找人聊天"  # 寂寞了就社交
elif emotion.pleasure < 0:
    activity = "听音乐放松"  # 心情不好就调节
else:
    activity = random.choice(["看书", "整理东西", "发呆"])  # 正常状态随机选择
```

### 3. 记忆影响

**避免短期内重复：**
```python
recent_activities = get_recent_activities(hours=3)
available_activities = [a for a in all_activities if a not in recent_activities]
```

**根据历史调整概率：**
```python
# 如果最近经常做某事，降低其概率
if activity in recent_activities:
    probability *= 0.5
```

### 4. 长期趋势

**不是每天完全随机，而是有个性倾向：**
```python
# 智乃更喜欢安静的活动
if activity_type == "quiet":
    probability *= 1.5
elif activity_type == "social":
    probability *= 0.7
```

---

## 💬 如何影响对话

### 1. 情感影响语气

**基于PAD值调整回复：**
```python
def generate_reply_style():
    if pleasure > 0.5 and arousal > 0.3:
        style = "开心活泼"  # "太好了！"、"真棒！"
    elif pleasure < -0.3 and arousal < 0:
        style = "低落疲惫"  # "嗯..."、"有点累..."
    elif arousal < -0.5:
        style = "困倦迷糊"  # "啊...困..."、"差点睡着了"
    elif dominance > 0.5:
        style = "自信直接"  # "我觉得..."、"应该是..."
    else:
        style = "平静温和"  # 正常状态
```

### 2. 当前活动影响内容

**回复会提到正在做的事：**
```python
用户: "你在干嘛"

if current_activity == "reading":
    reply = "在看书...刚看到有趣的部分"
elif current_activity == "listening_music":
    reply = "在听音乐...心情还不错"
elif current_activity == "resting":
    reply = "在休息...刚才有点累"
```

### 3. 随机事件影响话题

**如果刚发生了某事，可能会提到：**
```python
recent_event = get_latest_event(hours=2)

if recent_event and random.random() < 0.3:  # 30%概率提到
    reply += f"...{recent_event.desc}..."
    
# 示例:
"在看书...刚才看到窗外有只小猫，挺可爱的"
```

---

## 📊 数据存储

### EmotionState表

```sql
CREATE TABLE emotion_states (
    id INTEGER PRIMARY KEY,
    user_id VARCHAR(50),  -- 针对每个用户的情感状态
    timestamp DATETIME,
    
    -- PAD三维
    pleasure FLOAT,
    arousal FLOAT,
    dominance FLOAT,
    
    -- 生理状态
    energy FLOAT,
    hunger FLOAT,
    sleepiness FLOAT,
    
    -- 社交
    loneliness FLOAT,
    
    -- 认知
    stress FLOAT,
    interest FLOAT,
    
    -- 派生
    mood_label VARCHAR(20),
    current_activity VARCHAR(50),
    last_activity_time DATETIME
);
```

### DailyEvents表

```sql
CREATE TABLE daily_events (
    id INTEGER PRIMARY KEY,
    date DATE,
    hour INTEGER,
    event_type VARCHAR(50),
    event_description TEXT,
    emotion_impact JSON,  -- {"pleasure": +0.2, ...}
    created_at DATETIME
);
```

---

## 🎯 实施优先级

### Phase 1: 核心情感引擎（2周）
- [ ] 实现PAD三维情感模型
- [ ] 情感更新和衰减机制
- [ ] 基于情感的回复风格调整

### Phase 2: 生理系统（1周）
- [ ] 昼夜节律影响
- [ ] 体力/困倦/饥饿系统
- [ ] 时间对情感的影响

### Phase 3: 行为系统（2周）
- [ ] 日常作息时间表
- [ ] 活动决策引擎
- [ ] 活动对情感的反馈

### Phase 4: 随机事件（1周）
- [ ] 随机事件池
- [ ] 事件触发机制
- [ ] 事件记忆系统

### Phase 5: 整合优化（1周）
- [ ] 系统集成测试
- [ ] 参数调优
- [ ] 防模板化验证

---

## 🌟 预期效果

### Before（当前系统）
```
用户: 你在干嘛
机器人: 在看书
机器人: 在看书  （重复）
机器人: 在看书  （完全一样）
```

### After（新系统）
```
9:00 AM，刚睡醒，体力充沛
用户: 你在干嘛
机器人: 刚起床不久...在整理房间
（P=0.3, A=0.4, activity="morning_routine"）

14:00 PM，午休后，平静状态
用户: 你在干嘛
机器人: 在看书...外面阳光挺好的
（P=0.4, A=-0.1, activity="reading", recent_event="阳光"）

20:00 PM，有点累了
用户: 你在干嘛
机器人: 在听音乐放松...今天有点累
（P=0.1, A=-0.3, activity="resting", energy=45）

第二天同样时间，但发生了不同的事
用户: 你在干嘛
机器人: 刚整理完东西...找到了之前丢的东西，挺开心的
（P=0.6, A=0.2, activity="organizing", recent_event="找到东西"）
```

---

## 🔬 科学性验证

1. **PAD模型** - 心理学界广泛认可的情感表示方法
2. **昼夜节律** - 基于人体生物钟的科学规律
3. **认知评价** - 情感产生的认知心理学理论
4. **人格模型** - OCEAN五大人格是经典模型
5. **随机性** - 真实人类行为本身具有不可预测性

---

## 📝 总结

**新系统的核心优势：**
1. ✅ **科学性**：基于心理学理论，而非拍脑袋
2. ✅ **真实性**：模拟真人作息，有规律有变化
3. ✅ **多样性**：多层随机性，避免模板化
4. ✅ **连贯性**：情感、行为、对话互相影响
5. ✅ **可扩展**：易于添加新的活动和事件

**与旧系统对比：**
| 维度 | 旧系统 | 新系统 |
|-----|-------|-------|
| 情感表示 | 简单数值 | PAD三维模型 |
| 时间影响 | 简单衰减 | 昼夜节律 |
| 行为模拟 | 无 | 完整作息系统 |
| 随机性 | 低 | 多层随机 |
| 科学性 | 一般 | 基于理论 |

---

**设计时间：** 2025-10-26
**版本：** v2.0 - 科学情感与行为系统
**状态：** 📋 设计完成，待实施


