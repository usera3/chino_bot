# 消息撤回功能说明

## 📋 功能概述

机器人现在支持撤回自己发送的消息。当用户要求"撤回"、"删除消息"、"收回"时，AI 会自动调用撤回工具。

---

## 🎯 使用方法

### 1. **请求撤回最近一条消息**
```
用户: "撤回"
用户: "删除刚才的消息"
用户: "收回"
```

AI 会自动撤回最近发送的1条消息。

### 2. **请求撤回多条消息**
```
用户: "撤回最近3条消息"
用户: "删除刚才的两条消息"
```

AI 会撤回最近的多条消息（最多5条）。

---

## ⚠️ 限制

1. **时间限制**: 只能撤回2分钟内发送的消息
2. **数量限制**: 最多撤回5条消息
3. **权限限制**: 只能撤回机器人自己发送的消息
4. **记录限制**: 只保留最近10条消息的ID记录

---

## 🔧 技术实现

### 工具信息
- **工具名称**: `recall_message`
- **参数**:
  - `user_id` (必需): 用户QQ号
  - `count` (可选): 撤回数量，默认1条，最多5条

### 消息追踪
- 每次机器人发送消息后，会自动保存消息ID
- 消息ID存储在内存中（重启后清空）
- 每个用户最多保留最近10条消息记录

### AI 调用格式
```
[CALL_TOOL]
tool_name: recall_message
arguments: {"user_id": "1446437177", "count": 1}
[/CALL_TOOL]
```

---

## 📝 测试示例

### 示例1: 撤回单条消息
```
用户: 你好
智乃: 嗯，你好。

用户: 撤回
智乃: [调用撤回工具]
结果: 上一条"嗯，你好。"被撤回
```

### 示例2: 撤回多条消息
```
用户: 给我讲个笑话
智乃: [发送笑话，分3段]
      第一段...
      第二段...
      第三段...

用户: 不好笑，撤回这3条
智乃: [调用撤回工具，count=3]
结果: 上面3条消息被撤回
```

---

## 🎉 特点

1. **智能识别**: AI 自动理解"撤回"、"删除"等意图
2. **灵活数量**: 支持撤回指定数量的消息
3. **友好提示**: 如果撤回失败，会给出友好的提示
4. **安全设计**: 只能撤回自己的消息，不影响其他消息

---

## 🛠️ 文件结构

```
new-bot/
├── tools/
│   └── recall_tool.py          # 撤回工具实现
├── core/
│   └── function_calling_agent.py  # 工具注册
└── plugins/
    └── langchain_chat.py       # 消息ID追踪（可选）
```

---

## 💡 后续优化方向

1. **持久化存储**: 将消息ID保存到数据库，重启后仍可撤回
2. **群聊支持**: 在群聊中也能撤回自己的消息
3. **批量撤回**: 支持撤回某个时间段的所有消息
4. **定时撤回**: 发送后自动在N秒后撤回（阅后即焚）

---

**🎊 现在可以测试撤回功能了！** 🗑️




