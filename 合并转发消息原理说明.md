# 合并转发消息原理说明

## OneBot V11 合并转发 API

### API 端点

- `send_group_forward_msg` - 发送群聊合并转发消息
- `send_private_forward_msg` - 发送私聊合并转发消息

### 消息格式

合并转发消息使用 `node` 类型的消息段，每个 node 代表一条消息：

```json
[
    {
        "type": "node",
        "data": {
            "name": "发送者昵称",
            "uin": "发送者QQ号",
            "content": "消息内容"
        }
    }
]
```

### 参数说明

#### node.data 字段

- `name` (string): 发送者显示的昵称
- `uin` (string): 发送者的 QQ 号
- `content` (string | array): 消息内容
  - 可以是纯文本字符串
  - 也可以是消息段数组（支持图片、表情等）

### 示例

#### 简单文本消息

```python
messages = [
    {
        "type": "node",
        "data": {
            "name": "用户A",
            "uin": "123456",
            "content": "你好"
        }
    },
    {
        "type": "node",
        "data": {
            "name": "用户B",
            "uin": "654321",
            "content": "你也好"
        }
    }
]

# 发送到群聊
await bot.call_api(
    "send_group_forward_msg",
    group_id=群号,
    messages=messages
)

# 发送到私聊
await bot.call_api(
    "send_private_forward_msg",
    user_id=用户QQ,
    messages=messages
)
```

#### 复杂消息（包含图片）

```python
messages = [
    {
        "type": "node",
        "data": {
            "name": "用户A",
            "uin": "123456",
            "content": [
                {
                    "type": "text",
                    "data": {"text": "看这张图"}
                },
                {
                    "type": "image",
                    "data": {"file": "图片URL或路径"}
                }
            ]
        }
    }
]
```

## 常见问题

### 1. 为什么合并转发消息只显示一个人？

**可能原因**：

1. **NapCat/OneBot 实现问题**
   - 某些 OneBot 实现可能不完全支持自定义 `uin`
   - 可能会忽略 `uin` 字段，统一显示为发送者

2. **私聊限制**
   - 私聊的合并转发可能不支持自定义发送者
   - 只有群聊的合并转发才能正确显示多个发送者

3. **QQ 版本/架构问题**
   - 某些 QQ 版本或架构（如 ARM64）可能不完全支持
   - NapCat 报错：`PacketBackend 不支持当前QQ版本架构`

### 2. 如何验证是否支持？

**测试步骤**：

1. 先在**群聊**中测试（群聊支持度更好）
2. 检查 NapCat/go-cqhttp 日志是否有错误
3. 尝试不同的 OneBot 实现（go-cqhttp、Lagrange、LLOneBot）

### 3. 解决方案

#### 方案 A：只在群聊中使用

修改工具，只支持群聊的伪造消息：

```python
# 只在群聊中启用
if not target_group_id:
    return "❌ 伪造消息功能仅支持群聊"
```

#### 方案 B：使用其他 OneBot 实现

- **go-cqhttp**: 最成熟，支持度最好
- **Lagrange**: 新一代实现，支持 NTQQ
- **LLOneBot**: 基于 LiteLoaderQQNT

#### 方案 C：降级方案

如果合并转发不支持，可以：
1. 发送多条普通消息模拟对话
2. 生成图片形式的对话截图
3. 使用文本格式展示对话

## 技术限制

### OneBot 协议限制

1. **不支持转发套娃**：合并转发消息中不能再包含合并转发
2. **消息数量限制**：单次合并转发的消息数量可能有上限
3. **内容限制**：某些特殊消息类型可能不支持

### QQ 客户端限制

1. **显示限制**：QQ 客户端可能对合并转发消息的显示有特殊处理
2. **权限限制**：某些账号可能没有发送合并转发的权限
3. **频率限制**：频繁发送可能被限制

## 参考资料

- [OneBot V11 标准](https://github.com/botuniverse/onebot-11)
- [go-cqhttp 文档](https://docs.go-cqhttp.org/)
- [NapCat 文档](https://napneko.github.io/)
- [关于自定义合并转发消息的讨论](https://github.com/Mrs4s/go-cqhttp/discussions/926)

---

**更新时间**: 2025-01-19  
**版本**: v1.0
